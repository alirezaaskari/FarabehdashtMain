<?php

declare(strict_types=1);

return [

    // سقف پیوند خودکار: ۸ در هر ۱۰۰۰ کلمه، ولی متن کوتاه هم دست‌کم ۳ تا
    // می‌گیرد. بیشتر از این، متن به فهرست پیوند تبدیل می‌شود (DEC-31).
    'per_thousand_words' => 8,
    'min_per_document' => 3,

    // عبارت کوتاه‌تر از این پیوند نمی‌خورد؛ «صوت» و «گرد» در هر جمله‌ای هستند.
    'min_phrase_length' => 3,

];
