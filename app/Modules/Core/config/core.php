<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | دسته‌بندی‌های مشترک
    |--------------------------------------------------------------------------
    |
    | کلید فنی => نام فارسی. فهرست بسته است: اگر هر ماژول بتواند دسته‌بندی تازه
    | بسازد، شش ماه بعد پنج نام مختلف برای یک مفهوم داریم و فیلتر مشترک سایت
    | از کار می‌افتد.
    |
    */

    'taxonomies' => [
        'health_domain' => 'حوزه بهداشت حرفه‌ای',
        'industry' => 'صنعت',
        'content_type' => 'نوع محتوا',
    ],

    /*
    |--------------------------------------------------------------------------
    | سئو
    |--------------------------------------------------------------------------
    |
    | robots در محیط غیرproduction همیشه بسته است تا نسخه آزمایشی سایت به‌اشتباه
    | ایندکس نشود؛ این اتفاق برگشتش هفته‌ها طول می‌کشد.
    |
    */

    'seo' => [
        'sitemap' => [
            'cache_seconds' => (int) env('FBH_SITEMAP_CACHE_SECONDS', 3600),
        ],

        'robots' => [
            // مسیرهایی که هرگز نباید ایندکس شوند، حتی اگر ماژولشان عوض شود.
            'disallow' => [
                '/workspace',
                '/admin',
                '/login',
            ],
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | مدیا
    |--------------------------------------------------------------------------
    */

    'media' => [
        'disk' => env('FBH_MEDIA_DISK', 'public'),
        'max_size_kb' => 5120,
        'image_mimes' => ['image/jpeg', 'image/png', 'image/webp', 'image/svg+xml'],
        'document_mimes' => ['application/pdf'],
    ],

];
