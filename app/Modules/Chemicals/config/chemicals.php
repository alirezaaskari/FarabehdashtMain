<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | فهرست
    |--------------------------------------------------------------------------
    */

    'index' => [
        'per_page' => 12,
    ],

    /*
    |--------------------------------------------------------------------------
    | مقایسه‌گر
    |--------------------------------------------------------------------------
    |
    | «تا سه ماده» تصمیم پروتوتایپ است. بیش از این، جدول روی دسکتاپ هم عریض‌تر
    | از عرض قابل‌خواندن می‌شود.
    |
    */

    'compare' => [
        'max' => 3,
    ],

    /*
    |--------------------------------------------------------------------------
    | ابزارهای مرتبط
    |--------------------------------------------------------------------------
    |
    | این ابزارها به همه مواد مرتبط‌اند (تبدیل واحد عمومی است)، برخلاف بلوک
    | ابزار دانشنامه که به یک بخش خاص گره می‌خورد. از راه App\Contracts\ToolDirectory
    | خوانده می‌شوند تا وابستگی به ماژول ابزارها مستقیم نباشد (قاعده ۱).
    |
    */

    'related_tools' => [
        'ppm-to-mass-concentration',
        'mass-concentration-to-ppm',
        'twa-ppm',
    ],

    /*
    |--------------------------------------------------------------------------
    | ورود CSV
    |--------------------------------------------------------------------------
    |
    | سربرگ‌های ستون، دقیقاً همان‌هایی که خروجی CSV هم تولید می‌کند — تا فایلی
    | که از این سایت خارج شده، بدون تغییر همین‌جا دوباره وارد شود.
    |
    */

    'csv' => [
        'columns' => [
            'cas_number', 'name_fa', 'name_en', 'formula', 'molar_mass', 'physical_state',
        ],

        'max_rows' => 500,
    ],

];
