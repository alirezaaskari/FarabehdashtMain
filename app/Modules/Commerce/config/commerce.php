<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | کمیسیون
    |--------------------------------------------------------------------------
    |
    | نرخ پیش‌فرض هر جریان، به Basis Point (۱۰۰ = ۱٪). فقط وقتی به‌کار می‌رود
    | که هیچ ردیفی در commission_rates برای آن جریان ثبت نشده باشد — یعنی
    | همان روز نصب، پیش از اولین تنظیم دستی مدیر. تصمیم مدیر (ADR-0003):
    | فروشگاه و دوره‌ها ۲۰٪.
    |
    */
    'commission' => [
        'default_rate_bp' => [
            'shop' => 2000,
            'course' => 2000,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | درگاه پرداخت
    |--------------------------------------------------------------------------
    |
    | تصمیم مدیر: زرین‌پال (DEC-10). در محیط تست/محلی بدون کلید واقعی،
    | درایور fake به‌کار می‌رود تا هیچ تماس شبکه‌ای واقعی برقرار نشود.
    |
    */
    'payment' => [
        'driver' => env('FBH_PAYMENT_DRIVER', 'zarinpal'),

        'zarinpal' => [
            'merchant_id' => env('ZARINPAL_MERCHANT_ID'),
            'sandbox' => (bool) env('ZARINPAL_SANDBOX', false),
            'timeout' => 15,
        ],
    ],
];
