<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | نشانی پنل
    |--------------------------------------------------------------------------
    |
    | تصمیم مدیر (DEC-09): مسیر غیرقابل‌حدس. در متغیر محیطی می‌ماند تا بدون
    | تغییر کد عوض شود. امنیت واقعی از مجوزهاست، ولی مسیر ناشناخته حجم حمله
    | کورِ ربات‌ها را تقریباً صفر می‌کند.
    |
    */

    'path' => env('FBH_ADMIN_PATH', 'fbh-panel'),

    /*
    |--------------------------------------------------------------------------
    | امنیت حساب مدیر
    |--------------------------------------------------------------------------
    |
    | تأیید دومرحله‌ای در بخش امنیت پیاده می‌شود؛ این کلید از الان هست تا
    | جایش در پیکربندی مشخص باشد و بعداً جابه‌جا نشود.
    |
    */

    'require_two_factor' => (bool) env('FBH_ADMIN_2FA', false),

    /*
    |--------------------------------------------------------------------------
    | مشاهده به‌عنوان کاربر
    |--------------------------------------------------------------------------
    */

    'impersonation' => [
        'min_reason_length' => 10,
    ],

];
