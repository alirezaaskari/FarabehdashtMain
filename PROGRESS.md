# وضعیت پروژه فرابهداشت

> این فایل پس از هر تغییر معنادار به‌روز می‌شود. برای ادامه کار در نشست جدید، اول این فایل خوانده شود.

## مرحله فعلی

**خطای درگاه دیگر ۵۰۰ نیست** (شاخه `claude/project-thread-9j5tod`): روی هاست `ZARINPAL_MERCHANT_ID` خالی بود و «خرید پلن» ۵۰۰ می‌داد.
`ZarinPalGateway` حالا `App\Support\Payments\PaymentGatewayUnavailable` می‌اندازد (کلید خالی، رد درخواست، قطعی شبکه)؛
سه اکشن شروع پرداخت (اشتراک، سفارش، ثبت‌نام دوره) ردیف را «ناموفق» می‌کنند و کنترلرها خطا را گزارش و صفحه «پرداخت ناموفق» را با پیام عمومی نشان می‌دهند.
**پوسته پنل مدیریت** (شاخه `claude/project-thread-e12fte`): CSS آماده Filament کلاس‌های Tailwind
صفحه‌های سفارشی ماژول‌ها را نداشت، پس فرم‌ها و فهرست‌های پنل بی‌فاصله و درهم بودند. پوسته
`resources/css/filament/fbh/theme.css` (همان CSS پایه Filament + `@source` نماهای ماژول‌ها) با
`viteTheme` وصل و در `public/build` کامیت شد؛ منو در چهار گروه `App\Support\Admin\NavigationGroup`
با آیکون. گزارش بازبینی کامل بخش‌های ۱ تا ۱۷: `/mnt/project-files/audit/`.
**بسته امنیت و خرید** (همان شاخه، از همان گزارش): قرائت پروژه فقط تجهیز خود مالک را می‌پذیرد؛
خریدار از صفحه محصول و کارت «خریدهای من» دوباره دانلود می‌کند؛ پرداخت رهاشده دوره دیگر ثبت‌نام را
قفل نمی‌کند؛ کلیدهای «تک‌فروشی فایل/دوره» با قرارداد `SalesSwitch` و میان‌افزار `sales:` واقعاً
مسیرهای فروش را می‌بندند؛ درگاه زرین‌پال به `App\Support\Payments` و `config/payments.php` رفت
تا خاموشی فروشگاه پرداخت Pro و دوره را نشکند (PR #23 مرج شد).
**صف تأیید افزوده‌های تازه** (همان شاخه، تصمیم Alireza: «فقط بخش تازه در صف»): جلسه و سؤال تازه
دوره منتشرشده (`approved_at`) و نسخه تازه فایل محصول منتشرشده (`review_status`) تا تأیید مدیر به
دانشجو و خریدار نمی‌رسند، ولی خود دوره و محصول منتشر می‌مانند. تأیید و رد از بخش دوم صفحه‌های بررسی
پنل؛ رد یادداشت اجباری دارد (PR #25 مرج شد).
**محتوا از پنل** (همان شاخه): ویرایشگر دانشنامه (`ArticleResource`، `SaveArticle`، ارسال برای بازبینی،
انتشار) و ویرایشگر بانک مواد با حد مواجهه و منبع (`SubstanceResource`، `SaveSubstance`، انتشار). هر ذخیره
و انتشار از راه قرارداد تازه `RevisionEvent` نسخه می‌گیرد و محتوای منتشرشده با `LinkableContentChanged`
پیوندهای داخلی را همان لحظه بازسازی می‌کند. گام بعد طبق گزارش: بسته «میزکار کاربرپسند».
**صف «درخواست‌های نقش» در پنل** (شاخه `claude/project-thread-9j5tod`): در چک‌لیست دستی پذیرش معلوم شد
`ReviewProfileRequest` هیچ رابطی نداشت و روی سایت زنده کسی فروشنده یا مدرس نمی‌شد.
`Identity/Filament/Pages/ProfileRequestsPage` تأیید و رد (با یادداشت اجباری) را به مدیر محتوا می‌دهد.
**بخش ۱۷-ب — تست پذیرش نسخه ۱: ساخته شد** (شاخه `claude/section-17-plan-n4iu7e`، در انتظار بازبینی). کد «آماده انتشار» است؛ گزارش در `docs/acceptance/v1/`.
**بررسی طراحی و تطابق با پروتوتایپ** (شاخه `claude/design-review-y9ofee`).
بخش اول در #15 و #17 مرج شد: پیمایش موبایل (منوی کشویی، نوار پایین، `x-disclosure`)،
دستیار سه‌مرحله‌ای انتخاب ابزار (`Hazard`، `WorkStage`، پیشنهاد از `SearchSource`)،
صفحه اصلی دوستونی با مبدل سریع ppm، سربرگ و فوتر چهارستونی، و صفحه Pro با ستون رایگان و
جدول مقایسه. بخش دوم PR #18 (پیش‌نویس): داشبورد میزکار (`Dashboard::highlights/activity`،
«شروع کار با میزکار»)، ۴۰۴ با پوسته، `ToolAvailability::NotReviewed`، نسخه «۱٫۰»،
پنل «محاسبه با این ماده» که وزن مولکولی را پیش‌پر می‌کند، سوییچ WBGT داخل/بیرون
(`variant`/`alternative`) و تفسیر مخصوص هر گروه ابزار (`ToolCategory::interpretation`).
همان PR: ردیف «اخیراً استفاده‌شده» در صفحه ابزارها (`RecentTools`، از محاسبه‌های ذخیره‌شده
خود کاربر) و اقدام اول در حالت‌های خالی (سبد، فروش، جست‌وجو، گزارش‌ساز).
PR #20 (شاخه `claude/design-ideas-y9ofee`): جست‌وجوی فوری (`workspace.search.suggest`، تکه HTML
از همان `SiteSearch`، کلید `/`) و حالت میدانی ابزار (`?field=1`، فیلد ۵۴ پیکسل، فقط فرم و نتیجه).
گام بعد: ایده‌های ۲.۲ (نقطه‌های این جلسه)، ۲.۱۰، ۲.۹ از `design-review/ui-ux-ideas.md`. Alireza اجرای همه را به صلاح‌دید Claude سپرد (۲۰۲۶-۰۹-۲۴).
**بخش ۱۷-الف — سئو و موتور پیوند داخلی `Modules/Linking`: تحویل شد** (PR #14 مرج شد).
**بخش ۱۶ — گزارش‌ساز `Modules/Reports`: تحویل شد** (PR #13 مرج شد).
**بخش ۱۵ — میزکار و صفحات عمومی `Modules/Workspace`: تحویل شد** (PR #12 مرج شد).
**بخش ۱۴ — درآمدزایی `Modules/Monetization`: تحویل شد** (PR #11 مرج شد).
**بخش ۱۳ — دوره‌ها `Modules/Courses`: تحویل شد** (PR #9 مرج شد).
**هم‌ترازسازی دوم با پروتوتایپ: تحویل شد.** صفحه اصلی واقعی از روی
`Main.dc.html` ساخته شد و مقیاس تایپوگرافی معنایی در کل مخزن اجرا شد.
**بخش ۱۲ — تجارت `Modules/Commerce`: تحویل شد.**
**بخش ۱۱ — هسته مالی `Modules/Ledger`: تحویل شد.**
**بخش ۱۰ — بانک مواد شیمیایی `Modules/Chemicals`: تحویل شد.**
**استایل عقب‌مانده سایت اصلاح شد** (`public/build` حالا با گیت منتقل می‌شود، نه جدا).
**بخش ۹ — دانشنامه `Modules/Encyclopedia`: تحویل شد.**
**هم‌ترازسازی طراحی با پروتوتایپ: تحویل شد.** همه صفحه‌های ساخته‌شده (بخش ۲ تا ۸)
روی همان پوسته، مقیاس تایپوگرافی و گرامر کارتِ آرت‌بوردهای پروتوتایپ نشستند.
**بخش ۸ — پروژه و تجهیزات `Modules/Projects`: تحویل شد.**
**سایت روی هاست مستقر شد و کار می‌کند** (`https://farabehdasht.com`، حالت staging).
**CI برای اولین بار سبز شد** — اجرای شماره ۵ روی
[PR #3](https://github.com/alirezaaskari/FarabehdashtMain/pull/3).
بخش بعدی: خروج از staging، تصمیم مدیر پس از چک‌لیست دستی (DEC-36)؛ سپس بخش ۱۸ (نسخه ۱٫۱).

## آنچه انجام شده

- تصمیم خروج از وردپرس و بازنویسی کدمحور گرفته شد.
- به‌دلیل میزبانی روی cPanel، گزینه Next.js بررسی و رد شد؛ پشته **Laravel + Filament + MySQL**
  توسط مدیر تأیید شد (مسیر A).
- ADRهای زیر نوشته شد:
  - `docs/adr/ADR-0001-platform-and-stack.md` — پشته فنی و دلایل رد Next.js
  - `docs/adr/ADR-0002-account-and-profiles.md` — مدل تک‌حسابی، Profileها، حریم خصوصی کارجو
  - `docs/adr/ADR-0003-money-and-ledger.md` — دفتر کل دوطرفه و قواعد مالی
- فازبندی اجرایی در `docs/roadmap/phases.md` تدوین شد (فاز ۰ تا ۸).

- **پروتوتایپ بصری کامل** با **۶۵ آرت‌بورد** در ۱۲ ردیف (فاز ۰ تا ۸ + مرکز مدیریت +
  درآمدزاهای جدید + موبایل) روی بوم طراحی منتشر شد:
  https://claude.ai/artifact/5Q4w4K7Ju6scc8yjtmbL74
- سه جریان درآمدی جدید توسط مدیر تأیید شد: **اشتراک Pro**، **بسته‌های آزمون**، **صندلی تیمی**
  (`docs/roadmap/revenue-additions.md`).
- **مرکز مدیریت** به ۱۰ صفحه گسترش یافت، با اصول ثبت‌شده در `docs/architecture/admin-panel.md`
  (تاریخ اثر برای نرخ‌ها، پیش‌نمایش اثر مالی بازگشت وجه، نسخه‌بندی فرمول، ورود CSV،
  نقش‌های تفکیک‌شده، Audit Log غیرقابل تغییر).
- **۹ صفحه موبایل** با الگوهای اختصاصی ساخته شد (`docs/architecture/mobile.md`).
- پروتوتایپ به **۷۴ صفحه** رسید. ردیف تازه «استانداردهای طراحی» اضافه شد: حالت تاریک ·
  ابزار میدانی تاریک · کتابخانه حالت‌های خالی · حالت‌های بارگذاری · صفحات خطا ·
  استانداردهای عدد و متن · چک‌لیست دسترس‌پذیری · راهنمای اولین ورود.
- صفحه **درآمدزایی و ماژول‌ها** در پنل مدیریت ساخته شد: هر جریان درآمدی کلید مستقل دارد
  (`docs/architecture/monetization-toggles.md`).
- پروتوتایپ به **۸۲ صفحه** رسید. ده مورد تأییدشده ساخته شد: گذرگاه تبدیل به Pro ·
  صفحه وضعیت سرویس · نسخه چاپی مقاله · نشانگر تازگی داده · واردکننده رزومه ·
  بلوک ابزار درون متن · چک‌لیست چاپی · دفترچه تجهیزات · مقایسه دو دور اندازه‌گیری ·
  قالب پروژه بر اساس صنعت. مستندات: `data-freshness-and-print.md` و `measurement-tools.md`.

## بخش ۱ — اسکلت و CI (تحویل‌شده)

- Laravel 13.32 روی PHP 8.3 · MySQL
- سیستم ماژولار: `ModuleProvider` (سیم‌کشی خودکار مسیر، قالب، ترجمه، مهاجرت، پیکربندی)،
  `ModuleRegistry` (تنها مرجع شناخت ماژول‌ها)، `ModulesServiceProvider` (ثبت با شکست بلند)
- `config/modules.php` — فعال/غیرفعال‌کردن هر ماژول با یک خط
- دستور `php artisan fbh:make-module <Name>` با stubهای استاندارد
- ماژول نمونه `Health` با `GET /_health`
- Pint + PHPStan (Larastan سطح ۶) + PHPUnit · GitHub Actions
- README، CLAUDE.md با هفت قاعده غیرقابل نقض

**شواهد:** ۱۰ تست سبز، Pint تمیز، ساخت و فعال‌سازی و حذف ماژول آزمایش شد.

## بخش ۲ — لایه طراحی (تحویل‌شده)

- توکن‌ها در `resources/css/tokens.css`؛ هیچ رنگ هگزی در قالب‌ها نیست
- Vazirmatn خودمیزبان با `@fontsource-variable` — بدون CDN و بدون درخواست بیرونی
- Tailwind ۴ با نگاشت معنایی توکن‌ها و واریانت `dark` هماهنگ با `data-theme`
- سیزده کامپوننت Blade: icon · button · card · badge · field · alert ·
  empty-state · skeleton · permission-notice · stat · disclaimer · toggle · theme-toggle
- سه چیدمان: عمومی (قفل روشن، ایندکس‌پذیر) · میزکار (تاریک‌پذیر، noindex) · چاپ
- حالت تاریک با ذخیره انتخاب کاربر در `localStorage`
- `scripts/a11y-check.mjs` — بررسی خودکار سرریز، هدف لمسی، برچسب، ترتیب هدینگ و RTL

**شواهد:** ۲۵ تست سبز · بررسی دسترس‌پذیری در سه عرض روی دو صفحه قبول ·
صفحه `/design-system` رندر و بصری بازبینی شد.

## بخش ۳ — هویت و دسترسی (تحویل‌شده)

ماژول `app/Modules/Identity`. معیار پذیرش بخش، هر سه برآورده شد.

- **ورود با موبایل و کد یک‌بارمصرف.** ثبت‌نام جداگانه ندارد؛ اولین ورود موفق
  حساب را می‌سازد. کد خام هرگز ذخیره نمی‌شود، فقط هش آن.
- **سه لایه محدودیت:** فاصله تا ارسال بعدی · سقف ساعتی هر شماره · سقف ساعتی هر IP.
- **پروفایل‌ها:** پنج نقش روی یک حساب. فعال‌سازی همیشه با وضعیت `Pending` شروع
  می‌شود و فقط مدیر آن را `Active` می‌کند. غیرفعال‌کردن هیچ داده‌ای را حذف نمی‌کند.
- **مجوزها:** لایه بومی و جمع‌شونده (پایه ∪ پروفایل‌های فعال). هر مجوز خودکار یک
  Gate می‌شود. نگاشت و برچسب فارسی در `config/identity.php` است، نه در کد.
- **پیامک:** قرارداد `App\Contracts\SmsSender` با سه درایور `log`، `melipayamak`
  و `Fake` برای تست. تعویض درایور فقط یک متغیر محیطی است.
- **حریم خصوصی:** کد ملی رمزنگاری‌شده ذخیره می‌شود و برای تشخیص تکراری هش جداگانه
  دارد؛ صفحه ورود سه قول حریم خصوصی کارجو را صریح می‌گوید.
- سه صفحه: ورود · کد تأیید (با شمارش معکوس ارسال دوباره) · نقش‌ها و پروفایل‌ها.

**شواهد:** `composer run check` هر سه مرحله سبز — Pint تمیز · PHPStan سطح ۶ بدون
ایراد · ۷۱ تست سبز (۱۲ ورود · ۸ پروفایل · ۸ مجوز · ۱۷ واحد نرمال‌سازی موبایل ·
۱ فعال‌بودن ماژول · بقیه از بخش ۱ و ۲) · جریان کامل ورود تا فعال‌سازی نقش در مرورگر واقعی اجرا و
تصویربرداری شد · بررسی دسترس‌پذیری روی `/design-system`، `/login`،
`/login/verify` و `/workspace/profiles` در سه عرض قبول · حالت تاریک بازبینی شد.

**تحلیل ایستا، از «ریسک باز» به «اجراشده».** دانلود بسته از `codeload.github.com`
در این محیط ۴۰۳ می‌گیرد ولی خود `github.com` روی پروتکل git باز است؛ پس larastan و
phpstan از git کلون و به‌صورت مخزن `path` موقت نصب شدند و `composer.json` و
`composer.lock` دست‌نخورده ماندند. اولین اجرای واقعی سطح ۶ سیزده ایراد پیدا کرد —
از جمله اینکه کلید `checkMissingIterableValueType` در PHPStan ۲ حذف شده و پیکربندی
را نامعتبر می‌کرد، یعنی تحلیل حتی روی CI هم اجرا نمی‌شد. همه اصلاح شدند
(جزئیات در `docs/adr/ADR-0001-platform-and-stack.md`).

**چیزهایی که در همین بخش در لایه‌های پایین‌تر اصلاح شد:**

| ایراد | اصلاح |
|---|---|
| مسیرهای ماژول بدون گروه میان‌افزار `web` بار می‌شدند (نشست کار نمی‌کرد) | `ModuleProvider::bootRoutes` حالا `web` را به مسیرهای وب و `web + admin` را به مسیرهای مدیریت می‌بندد |
| `users.name` اجباری بود ولی ثبت‌نام فقط با موبایل است | در مهاجرت ماژول nullable شد |
| عدد لاتین داخل متن فارسی | دستور `@fa` و کلاس `App\Support\PersianDigits`؛ شماره‌گذاری `ol` هم با `list-style-type: persian` |
| سربرگ میزکار در ۳۹۰ پیکسل سرریز می‌کرد | دکمه «حساب من» روی موبایل فقط آیکون شد |
| صفحه کد تأیید H1 نداشت | کامپوننت `card` پارامتر `level` گرفت |
| بررسی دسترس‌پذیری، ورودی پنهان CSRF و چک‌باکس داخل برچسب بزرگ را اشتباه می‌گرفت | `scripts/a11y-check.mjs` اصلاح شد |

## بخش ۴ — زیرساخت‌های عرضی (تحویل‌شده)

ماژول `app/Modules/Core` به‌علاوه چند شیء مقدار در `app/Support`.
تصمیم‌های ساختاری در `docs/adr/ADR-0004-cross-cutting-infrastructure.md`.

- **دفتر رویداد غیرقابل تغییر.** `AuditLog::update()` و `delete()` استثنا پرتاب
  می‌کنند و جدول `updated_at` ندارد. دفتری که بشود دستکاری‌اش کرد، دفتر نیست.
- **ثبت از راه قرارداد.** ماژول یک `AuditableEvent` منتشر می‌کند و اصلاً نمی‌داند
  Core وجود دارد. شنونده روی خود اینترفیس ثبت شده، پس ماژول تازه سیم‌کشی
  نمی‌خواهد. پنج رویداد Identity به همین شکل وصل شدند (قاعده ۷ پروژه).
- **دفتر، انبار داده شخصی نیست.** ردیف ورود شناسه کاربر را دارد نه شماره موبایلش؛
  یک تست خودکار این را بررسی می‌کند.
- **نسخه‌بندی محتوا.** مدل با `Revisable` خودش می‌گوید چه فیلدهایی ارزش
  نسخه‌برداری دارند — نه `view_count` و نه `updated_at`.
- **دسته‌بندی مشترک** با فهرست بسته در `config/core.php`: `health_domain` ·
  `industry` · `content_type`، یکسان روی همه محتواها.
- **تنظیمات مدیر** با ذخیره JSON تا نوع اصلی حفظ شود و کلید درآمدی اشتباه
  خاموش نشود.
- **لایه سئو:** `SeoMeta` · `Schema` (مقاله، دوره، محصول، پرسش متداول، مسیر
  راهبری) · `SitemapBuilder` · `GET /sitemap.xml` · `GET /robots.txt`.
- **شش صفحه خطای فارسی** (۴۰۳، ۴۰۴، ۴۱۹، ۴۲۹، ۵۰۰، ۵۰۳) که همیشه راه خروج می‌دهند.
- **`Money`** (تومان صحیح، مرز درگاه، کمیسیون)، **`JalaliDate`** (بدون بسته
  جانبی، با `intl`) و **`PersianNumber`** در `app/Support`.

**شواهد:** `composer run check` هر سه مرحله سبز — Pint تمیز · PHPStan سطح ۶ بدون
ایراد · **۱۵۵ تست سبز** (۱۳ سئو · ۹ نسخه‌بندی · ۸ دفتر رویداد · ۸ رویدادهای
Identity · ۷ تنظیمات · ۷ دسته‌بندی · ۴ صفحه خطا · ۲ فعال‌بودن ماژول · ۱۷ پول ·
۹ تاریخ شمسی · بقیه از بخش‌های پیشین) · `/robots.txt`، `/sitemap.xml` و صفحه
۴۰۴ روی سرور واقعی بررسی و تصویربرداری شدند · بررسی دسترس‌پذیری روی سه صفحه در
سه عرض قبول.

**اثبات جداسازی:** با برداشتن `'Core'` از `config/modules.php`، تنها تست‌هایی که
می‌شکنند تست‌های خود ماژول Core هستند (۴۵ مورد). تست‌های Identity، Health و
سطح برنامه همگی سبز می‌مانند — یعنی ماژول‌ها واقعاً از راه قرارداد و رویداد حرف
می‌زنند، نه با import مستقیم.

**ایرادی که فقط با اجرای واقعی پیدا شد:** تست‌های `robots.txt` سبز بودند ولی روی
سرور، مسیر اصلاً اجرا نمی‌شد — وب‌سرور `public/robots.txt` را پیش از رسیدن به PHP
جواب می‌دهد و فایل پیش‌فرض لاراول می‌گوید «همه‌چیز آزاد». فایل حذف شد و تستی
اضافه شد که وجود نداشتنش را بررسی می‌کند.

**یک اشکال واقعی که تست پیدا کرد:** `SettingsRepository::set()` بدون گروه،
دسته‌بندی و توضیحی را که مدیر قبلاً گذاشته بود پاک می‌کرد.

## بخش ۵ — مرکز مدیریت پایه (تحویل‌شده)

ماژول `app/Modules/Admin` روی Filament 5.8.

- **معیار پذیرش برآورده شد:** مدیر محتوا هیچ توانایی مالی ندارد و مدیر مالی
  محتوا منتشر نمی‌کند. چهار نقش: ارشد · محتوا · مالی · کاریابی.
- **نقش مدیریتی، پروفایل نیست.** جدول `admin_roles` از `user_profiles` جداست و
  فقط مدیر ارشد اعطا می‌کند؛ هیچ مسیر عمومی درخواستی ندارد.
- **اولین مدیر** فقط با `php artisan fbh:make-admin 09xxxxxxxxx` ساخته می‌شود.
  صفحه‌ای که بتواند مدیر بسازد، دیر یا زود پیدا می‌شود.
- **داشبورد تصمیم‌محور** با صف یکپارچه تأیید در صدر صفحه. هر ماژول از راه
  قرارداد `ApprovalQueueSource` مورد می‌دهد و هر مدیر فقط چیزی را می‌بیند که
  توانایی تصمیم‌گیری درباره‌اش را دارد.
- **دفتر رویداد** فقط‌خواندنی، از راه قرارداد `AuditTrailReader` — Admin مدل
  Core را import نمی‌کند.
- **مشاهده به‌عنوان کاربر** با دلیل اجباری، ثبت خودکار، و مسدودبودن عملیات مالی.
  مسیر بازگشت توانایی لازم ندارد تا کسی در این حالت گیر نکند.
- **نشانی پنل** از `FBH_ADMIN_PATH` می‌آید (DEC-09).

**شواهد:** `composer run check` سبز — Pint · PHPStan سطح ۶ بدون ایراد ·
**۲۱۰ تست** (۱۶ تفکیک نقش‌ها · ۱۲ مشاهده به‌عنوان کاربر · ۸ دسترسی پنل ·
۷ صف تأیید · ۷ دستور ساخت مدیر · بقیه از بخش‌های پیشین) · پنل در مرورگر واقعی
با ورود کامل تست و تصویربرداری شد · بررسی دسترس‌پذیری قبول · با غیرفعال‌کردن
ماژول، فقط تست‌های خودش می‌شکنند.

**چهار ایراد واقعی که فقط با اجرا پیدا شدند:**

| ایراد | اصلاح |
|---|---|
| `blade-ui-kit/blade-icons` (وابسته Filament) کامپوننت `<x-icon>` ما را پنهان می‌کرد و صفحات ۵۰۰ می‌دادند | نام پیش‌فرض آن بسته در `config/blade-icons.php` عوض شد |
| مدیرِ ساخته‌شده با کنسول نام ندارد و Filament نام را رشته اجباری می‌خواهد → ۵۰۰ | `User` قرارداد `HasName` را پیاده کرد؛ نبودِ نام، شماره **نیمه‌پوشیده** می‌دهد |
| `/admin/impersonate/stop` با پارامتر `{user}` تطبیق می‌خورد و «stop» را شناسه می‌فهمید | ترتیب مسیرها عوض شد و `{user}` فقط عدد می‌پذیرد |
| پنل نام کاربر را به `ui-avatars.com` می‌فرستاد و فونتش حرف فارسی نداشت | آواتار محلی SVG و وزیرمتن خودمیزبان؛ پنل هیچ درخواست بیرونی ندارد |

**یک ساده‌سازی:** میان‌افزار `EnsureAdminAccess` حذف شد. Filament زودتر از آن
تصمیم می‌گرفت و عملاً هرگز اجرا نمی‌شد — دو نگهبان برای یک کار یعنی روزی یکی‌شان
عوض می‌شود و دیگری بی‌سروصدا بی‌اثر می‌ماند.

## استقرار اول روی هاست — انجام شد

`https://farabehdasht.com` · سرور `h9.hostdl.com` · حساب `weeamore` ·
`APP_ENV=staging` (پس `robots.txt` کل سایت را بسته و گوگل چیزی نمی‌بیند).

جریان کامل روی سرور واقعی تأیید شد: ورود با موبایل ← کد یک‌بارمصرف ← نشست ←
نقش مدیریتی ← پنل مدیریت.

| مورد | وضعیت |
|---|---|
| PHP | 8.4.25 (`ea-php84`)، همه افزونه‌ها موجود |
| دیتابیس | MariaDB 10.11.6، مهاجرت‌ها اجرا شد |
| Document Root | `farabehdasht.com/public` — کد و `.env` بیرون از ریشه وب |
| دارایی‌ها | روی کامپیوتر ساخته و منتقل شد (Node روی سرور نیست) |
| حساب مدیر | با `fbh:make-admin` ساخته شد |
| Cron | تنظیم شد — `schedule:run` هر دقیقه |
| SSL | فعال |

**بررسی امنیتی از بیرون:** `.env` و `storage/logs` → ۴۰۳ ·
`composer.json` و `vendor/` و `artisan` → ۴۰۴. هیچ فایل حساسی از وب خوانده نمی‌شود.

### چهار ایرادی که فقط استقرار واقعی نشان داد

هیچ‌کدام را تست نمی‌توانست بگیرد؛ همه به تفاوت محیط ساخت و محیط اجرا برمی‌گشتند.

| ایراد | علت ریشه‌ای | اصلاح |
|---|---|---|
| `composer install` روی سرور شکست خورد | `composer.lock` با PHP محیط من (۸٫۴) ساخته شده بود، نه با نسخه اعلام‌شده | `config.platform.php` قفل شد؛ پروژه به PHP ۸٫۴ رفت (ADR-0001) |
| اتصال دیتابیس با خطای ۱۱۳۰ رد شد | `DB_HOST=127.0.0.1` از TCP می‌رود، ولی سی‌پنل کاربر را `user@localhost` می‌سازد | نمونه `.env` به `localhost` اصلاح شد |
| کد ورود هیچ‌وقت در لاگ ظاهر نشد | `LogSmsSender` در سطح `info` می‌نوشت و `LOG_LEVEL=warning` آن را دور می‌ریخت | سطح به `warning` رفت — درایوری که کارش دیده‌شدن است نباید خاموش‌شدنی باشد |
| پوشه دامنه یک وردپرس زنده داشت | راهنما فرض گرفته بود خالی است | سه محافظ اضافه شد: حذف ممنوع، دیتابیس قدیمی دست‌نخورده، دستور برگشت فوری |

### روند به‌روزرسانی از این به بعد

```bash
cd ~/farabehdasht.com
git pull origin claude/pensive-brahmagupta-p5qa0o
# در صورت تغییر ظاهر، public/build تازه هم منتقل شود
bash scripts/deploy.sh
```

### رخداد ۲۰۲۶-۰۹-۲۴: `.env` سرور نسخه `.env.example` بود

سایت فقط از کش تنظیمات اجرا می‌شد؛ `optimize:clear` پشت‌سرهم ۴۰۴، نبود `APP_KEY` و
خطای ۱۱۳۰ دیتابیس را نشان داد، با `APP_DEBUG=true` روی سایت زنده. `.env` قدم‌به‌قدم
بازسازی شد (نام واقعی دیتابیس `weeamore_farabehdashtdata`) و پشتیبانش در
`~/env-backup-farabehdasht` است. پیامد در کد:

- `deploy.sh` اگر `APP_ENV` غیر از production/staging یا `APP_DEBUG` روشن باشد **متوقف** می‌شود (قبلاً فقط هشدار).
- پنل صفحه ورود ایمیل و رمز Filament را ندارد؛ مهمان به `/login` سایت می‌رود و پس از ورود با کد به همان پنل برمی‌گردد (`redirect()->intended`).
- راهنمای استقرار: گرفتن پشتیبان `.env` بلافاصله بعد از `key:generate`، و نام دیتابیس از سی‌پنل.

## بازبینی بخش ۱ تا ۵ — پیش از شروع بخش ۶

سه چیز پیدا شد. دو تا بسته شد، یکی باز است.

### ۱. CI هرگز سبز نشده بود — اصلاح شد

`main` قرمز است. هر دو اجرای CI در مرحله `composer install` شکست خورده‌اند،
با **همان علت ریشه‌ای** که روی هاست دیدیم: `composer.lock` برای PHP ۸٫۴ ساخته
شده بود ولی CI روی ۸٫۳ می‌دوید.

این را در کامیت `eec41f1` اصلاح کردم (قفل `platform.php` + بردن CI به ۸٫۴)، ولی
CI فقط روی `main` و روی Pull Request اجرا می‌شود، پس هنوز روی اصلاح نچرخیده.

**به‌روزرسانی — CI حالا واقعاً سبز است.** پس از باز شدن PR #3 سه اجرای دیگر
لازم شد تا برسد، و هر سه یک ایراد واقعی بود که فقط روی runner دیده می‌شد:

| اجرا | خطا | ریشه |
|---|---|---|
| ۳ | `Vite manifest not found` در ۱۲ تست | CI هیچ‌وقت `npm run build` نمی‌زد و `public/build` در gitignore است، پس محلی سبز و روی CI قرمز |
| ۴ | `npm ci` با `EUSAGE` | `playwright` در بخش ۲ به `package.json` اضافه شده بود ولی `package-lock.json` بازتولید نشده بود؛ تا اولین `npm ci` کسی نفهمید |
| ۵ | — | **سبز** |

درس مشترک هر سه: تفاوت محیط محلی و runner را فقط اجرای واقعی نشان می‌دهد.

برچسب مرحله تست هم از «Pest» به «PHPUnit» اصلاح شد؛ از بخش ۱ اشتباه مانده بود.

### ۲. دو موردی که از بخش ۴ به بخش ۵ موکول شدند و نیامدند

در بخش ۴ نوشتم «اعلان» و «سرویس آپلود مدیا» به بخش ۵ منتقل می‌شوند. بخش ۵
هیچ‌کدام را نساخت. این اشتباه در ثبت من بود، نه تصمیمی که گرفته شده باشد.

وضعیت واقعی: جدول و مدل `media_items` آماده است، ولی سرویس آپلود نوشته نشده.
صندوق اعلان هم اصلاً شروع نشده.

هیچ‌کدام مانع بخش ۶ نیستند و هر دو اولین مصرف‌کننده واقعی‌شان در بخش‌های
محتوایی (۹ به بعد) است. به همان‌جا منتقل شدند — این بار با تاریخ مشخص.

### ۳. «راهنمای اولین ورود»

از بخش ۳ به بخش ۴ موکول شد و آن هم نیامد. جایش بخش میزکار است، نه زیرساخت.
به بخش‌های محتوایی منتقل شد.

## بخش ۶ — موتور محاسبات (تحویل‌شده)

پکیج مستقل `packages/calc-engine`، فضای‌نام `Farabehdasht\CalcEngine`.
PHP خالص؛ `require` آن چیزی جز `php` ندارد.

- **معیار پذیرش برآورده شد:** تست طلایی سبز است و **تغییر فرمول بدون نسخه
  تازه ممکن نیست** — نه به‌عنوان قاعده نوشتاری، بلکه با سه نگهبان روی هم.
- **سه نگهبان:** رجیستری ثبت دوباره یک `id@version` را رد می‌کند · تست طلایی
  خروجی را با موردهای مرجع می‌سنجد · قفل `formulas.lock.json` اثر انگشت رفتاری
  هر نسخه را نگه می‌دارد.
- **چرا لایه سوم لازم بود:** اگر کسی ضریب را عوض کند، تست طلایی قرمز می‌شود —
  ولی وسوسه‌انگیزترین راه سبزکردنش عوض‌کردن خود مقادیر مرجع است. اثر انگشت از
  **نتیجه اجرای واقعی** ساخته می‌شود، نه از عدد نوشته‌شده در فایل، پس آن مسیر
  هم بسته است.
- **نسخه در نام کلاس است** (`WbgtIndoorV1`)، نه در یک ستون پایگاه داده. تغییر
  رابطه یعنی کلاس `V2` کنار نسخه قبلی؛ نسخه قدیمی هیچ‌وقت حذف نمی‌شود چون
  محاسبه‌های ذخیره‌شده به آن ارجاع می‌دهند.
- **موتور خالص است:** `compute()` نه ساعت می‌خواند، نه تصادف، نه فایل.
  `Calculation` هم مهر زمان ندارد؛ زمان کار لایه ذخیره‌سازی است (بخش ۷).
- **هر عدد واحد دارد.** `Quantity` بدون ذکر واحدِ انتظار مقدارش را نمی‌دهد و
  موتور هیچ واحدی را ضمنی تبدیل نمی‌کند.
- **قاعده محصولی در کد نشسته:** `Calculation::DISCLAIMER` روی هر محاسبه‌ای
  می‌آید و هر فرمول `limitations()` اجباری دارد. تستی هست که خروجی هیچ فرمولی
  واژه‌های ادعای انطباق را نداشته باشد.
- **سه رابطه در نسخه ۱٫۰٫۰:** `wbgt-indoor` · `wbgt-outdoor` · `sound-pressure-sum`.

**شواهد:** `composer run check` سبز — Pint · PHPStan سطح ۶ بدون ایراد ·
**۲۷۹ تست** (۶۹ تای آن‌ها موتور محاسبات) · و مهم‌تر، قفل **در عمل آزموده شد**:

| کاری که شد | نتیجه |
|---|---|
| ضریب `0.7/0.3` به `0.6/0.4` عوض شد | ۶ تست قرمز (۴ طلایی + قفل + رفتار) |
| ضریب عوض شد **و** هر پنج مقدار مرجع هم «اصلاح» شد تا سبز شود | تست طلایی سبز، **تست قفل همچنان قرمز** |
| فقط متن فارسی یک یادداشت عوض شد | همه سبز — ویرایش نگارشی نسخه نمی‌گیرد |

**دو تصمیمی که جای توضیح دارند:**

- **WBGT دو فرمول است، نه یکی با کلید انتخاب.** ورودی‌هایشان یکی نیست (حالت
  آفتابی دمای خشک هوا را هم می‌خواهد) و هرکدام باید مستقل نسخه بگیرد. در بخش ۷
  هر دو زیر یک «ابزار WBGT» نمایش داده می‌شوند؛ آن تصمیم لایه نمایش است.
- **وصل‌شدن با `repository` از نوع `path`**، نه psr-4 دستی در ریشه. این‌طور پکیج
  `composer.json` خودش را دارد و `StandaloneTest` می‌تواند بررسی کند که
  وابستگی‌اش چیزی جز `php` نیست — وگرنه ادعای «بدون Laravel» نگهبان نداشت.

**درباره مقادیر مرجع — صادقانه:** این‌ها مثال حل‌شده کپی‌شده از یک نشریه
نیستند. هر مورد، اجرای دستی رابطه منتشرشده (ISO 7243 برای WBGT، تعریف
لگاریتمی تراز برای صدا) است و حسابش کنارش در `derivation` نوشته شده تا با
ماشین‌حساب بازبینی شود. مورد چندباندی صدا از مسیر مستقلِ ترکیب دوبه‌دو و در
اجرایی جدا از این پکیج راستی‌آزمایی شد. اگر مدیر بخواهد مقادیر از یک مثال
حل‌شده رسمی گرفته شود، باید نشریه‌اش تهیه شود — این کار انجام نشده.

مستندات: `packages/calc-engine/README.md` · `docs/adr/ADR-0005-formula-versioning.md`

## بخش ۷ — ابزارها (تحویل‌شده)

ماژول `app/Modules/Tools` به‌علاوه نه رابطه تازه در موتور محاسبات.
**سیزده ابزار** روی **دوازده رابطه**، در پنج گروه.

- **معیار پذیرش برآورده شد و با نسخه دوم واقعی آزموده شد.** تستی که فقط یک
  محاسبه را دوباره اجرا کند چیزی ثابت نمی‌کند؛ وقتی نسخه دومی نباشد، هر دو
  اجرا طبیعتاً یکی‌اند. در `ReproducibilityTest` نسخه دوم واقعاً در موتور ثبت
  می‌شود و با همان ورودی عدد ۳۰ می‌دهد به‌جای ۲۸ — محاسبه ذخیره‌شده همچنان
  ۲۸ را برمی‌گرداند و محاسبه تازه ۳۰ را.
- **ابزار با فرمول یکی نیست.** `stel-ppm` و `twa-ppm` روی یک رابطه سوارند و
  فقط پنجره زمانی و راهنمایشان فرق می‌کند. تعریف ابزار در کد می‌ماند
  (`config/tools.php`) و فقط وضعیتش در جدول `tools` می‌نشیند.
- **فرم از قرارداد ورودی خود فرمول ساخته می‌شود.** هیچ فیلدی دستی تعریف نشده.
  افزودن ورودی به یک رابطه، خودبه‌خود فرمش را هم به‌روز می‌کند.
- **دو قاعده در تبدیل ورودی:** چیزی که عدد نیست عدد نمی‌شود (`(float) 'سه'` در
  PHP صفر است و همین یک تبدیل خاموش می‌تواند گزارش مواجهه را غلط کند)، و فیلد
  خالی حذف می‌شود نه اینکه صفر شود. ارقام فارسی و جداکننده اعشار فارسی
  پذیرفته می‌شوند.
- **ذخیره، ورودی‌ها را دوباره اجرا می‌کند** و به خروجی‌ای که مرورگر فرستاده
  اعتماد نمی‌کند.
- **جدول محاسبه‌ها فقط افزودنی است:** `update()` و `delete()` در سطح کد
  استثنا می‌اندازند و ستون `updated_at` ندارد.
- **دفتر رویداد مقادیر اندازه‌گیری را نمی‌نویسد** — داده میدانی می‌تواند
  محرمانه کارفرما باشد. فقط شناسه، ابزار و نسخه ثبت می‌شود.
- **ساختن نسخه تازه از پنل ممکن نیست.** پنل فقط بین نسخه‌های موجود انتخاب
  می‌کند و پیش از تغییر، تعداد محاسبه‌های متأثر را نشان می‌دهد.
- **دستیار انتخاب ابزار** فهرستی از موقعیت‌های واقعی میدانی است، نه درخت تصمیم:
  «چند دستگاه هم‌زمان کار می‌کنند» را کاربر می‌فهمد، «جمع لگاریتمی» را نه.

**شواهد:** `composer run check` سبز — Pint · PHPStan سطح ۶ بدون ایراد ·
**۴۸۰ تست** (۴۹ تای آن‌ها ماژول ابزارها، ۱۶۴ موتور محاسبات)، ۱۶۵۱ assertion ·
صفحه‌ها در مرورگر واقعی رندر و تصویربرداری شدند · `npm run a11y` روی چهار صفحه
تازه و سه عرض، قبول · با غیرفعال‌کردن ماژول فقط تست‌های خودش می‌شکنند.

**سه ایراد واقعی که فقط با اجرا پیدا شدند:**

| ایراد | اصلاح |
|---|---|
| کامپوننت‌های ناشناس ماژول در `resources/views/partials` پیدا نمی‌شدند | Blade برای فضای‌نام ماژول، پوشه `components/` می‌خواهد؛ جابه‌جا شدند |
| کشف صفحه‌های Filament ماژول‌ها پیش از `id()` پنل اجرا می‌شد و **هر صفحه سایت** ۵۰۰ می‌داد | کشف به آخر زنجیره منتقل شد. تست‌ها سبز بودند و فقط درخواست واقعی نشانش داد |
| «بازتولید نشد» روی محاسبه‌ای کاملاً سالم | عدد ۲۸٫۰ در JSON به‌صورت `28` نوشته و **عدد صحیح** خوانده می‌شود؛ مقایسه `===` آن را ناهمسان می‌دید. مقایسه عددی شد |

**دو اصلاح کوچک در لایه طراحی:** آیکون `calculator` در بخش ۲ عملاً خط ضربان
قلب بود (به `pulse` منتقل شد و آیکون واقعی ماشین‌حساب جایش نشست)؛ پیوند مسیر
صفحه کمتر از ۴۴ پیکسل ارتفاع داشت و بررسی دسترس‌پذیری گرفتش.

**نیازمند تصمیم مدیر — DEC-19:** مبنای حجم مولی در تبدیل ppm ↔ mg/m³. قانون
گاز کامل پیاده شد (۲۴٫۴۶۵ در شرایط مرجع) و با جدول‌های مرسوم که ۲۴٫۴۵ را
به‌کار می‌برند حدود ۰٫۰۶٪ فرق دارد. جزئیات در `docs/decisions-pending.md`.

مستندات: `app/Modules/Tools/README.md`

## بخش ۸ — پروژه و تجهیزات (تحویل‌شده)

ماژول `app/Modules/Projects`. پروژه اندازه‌گیری با ایستگاه و دور، دفترچه
تجهیزات، مقایسه دو دور، و تقویم الزامات پایش.

**هر دو نیمه معیار پذیرش برآورده شد:**

- **«تجهیز منقضی پیش از صدور گزارش هشدار می‌دهد.»** چهار حالت کالیبراسیون، و
  دو تفکیکش عمدی است: «ثبت‌نشده» با «معتبر» یکی نیست (وگرنه کافی بود کاربر
  تاریخ را خالی بگذارد تا هشدار خاموش شود)، و «نزدیک انقضا» حالت سوم است نه
  یک پرچم.
- **«مقایسه دو دور جدول و نمودار هم‌زمان می‌دهد.»** هر دو از یک شیء ساخته
  می‌شوند تا هیچ‌وقت دو چیز متفاوت نگویند؛ تستی همین را قفل می‌کند.

**تصمیم‌هایی که جای توضیح دارند:**

- **هشدار جلوی کار را نمی‌گیرد، تأیید می‌خواهد.** مسدودکردن کامل یعنی کاربر
  می‌رود تاریخ کالیبراسیون را الکی عوض می‌کند. تأیید در دفتر رویداد ثبت
  می‌شود و **با هر تغییر در تجهیزات پروژه باطل می‌شود** — وگرنه یک بار
  تأییدکردن، همه هشدارهای آینده را هم خاموش می‌کرد.
- **دو دور با واحد متفاوت مقایسه نمی‌شوند.** مقایسه دسی‌بل با لوکس عددی ممکن
  است و کاملاً بی‌معنا؛ نمودارش هم خوش‌شکل درمی‌آید و همین خطرناکش می‌کند.
- **جهت تغییر گزارش می‌شود، نه مطلوب‌بودنش.** برای صدا کاهش مطلوب است و برای
  روشنایی معمولاً افزایش.
- **مرز با ماژول ابزارها از راه قرارداد `CalculationReader`.** مقدار قرائت
  **کپی** می‌شود نه ارجاع؛ پروژه‌ای که عددش به جدول ماژول دیگری وابسته باشد،
  با خاموش‌شدن آن ماژول بی‌معنا می‌شود.
- **تقویم موارد گذشته را هم نشان می‌دهد** — تقویمی که فقط آینده را نشان بدهد،
  آنچه از دستتان رفته را پنهان می‌کند.

**شواهد:** `composer run check` سبز — Pint · PHPStan سطح ۶ بدون ایراد ·
**۵۴۸ تست** (۵۴ تای آن‌ها این ماژول)، ۱۸۴۸ assertion · جریان کامل ورود و
صفحه‌ها در مرورگر واقعی تصویربرداری شد · `npm run a11y` روی پنج صفحه میزکار و
سه عرض، قبول.

**پنج ایراد واقعی که با اجرا و بررسی پیدا شدند:**

| ایراد | اصلاح |
|---|---|
| **پالت نمودار در حالت تاریک مردود بود** — `#e08d5a` بیرون از باند روشنایی، و جدایی کوررنگی ΔE ۶٫۸ (مرزی) | پالت تاریک به `#d87109`/`#29a895` عوض شد: همه بررسی‌ها قبول، ΔE ۱۴٫۷. ادعای قبلی «هر شش بررسی قبول» فقط برای حالت روشن درست بود |
| صفحه‌های خصوصی از **چیدمان عمومی** استفاده می‌کردند: به کاربر واردشده دکمه «ورود» نشان می‌دادند و حالت تاریک اصلاً فعال نمی‌شد | به `x-layouts.workspace` منتقل شدند |
| برچسب محور در SVG بریده می‌شد؛ «96.4» فقط «4» دیده می‌شد | فضای SVG ذاتاً چپ‌به‌راست است و جهت صفحه را به ارث می‌برد؛ `[direction:ltr]` روی خود SVG |
| درصد تغییر با چهار رقم اعشار («۶٫۰۲۸۳٪») | `MeasurementNumber::format()` دقت اختیاری گرفت؛ درصدها یک رقم |
| `MeasurementNumber` داخل ماژول ابزارها بود و ماژول پروژه‌ها به آن نیاز داشت — نقض قاعده ۱ | به `app/Support/Measurement` منتقل شد |

**یک بهبود ابزار:** `npm run a11y` تا امروز هیچ صفحه میزکاری را بررسی نکرده
بود، چون فقط GET ساده می‌زد و به صفحه ورود هدایت می‌شد — و نتیجه سبزش درباره
آن صفحات **بی‌معنا** بود. حالا با `FBH_A11Y_MOBILE` اول با رمز یک‌بارمصرف وارد
می‌شود، و اگر صفحه‌ای با وجود ورود هدایت شد، صریح می‌گوید بررسی نشد.

مستندات: `app/Modules/Projects/README.md`

## هم‌ترازسازی طراحی با پروتوتایپ (تحویل‌شده)

نقد مدیر درست بود: صفحه‌ها «شبیه» پروتوتایپ بودند، نه «همان». مبنای کار، خواندن
مستقیم آرت‌بوردهای پروتوتایپ بود، نه حافظه: `DesignSystem`، `Main`، `Tool`،
`ToolsIndex`، `ToolFinder`، `SavedCalculations`، `ProjectTemplates`،
`MeasurementCompare`، `EquipmentRegister`، `ComplianceCalendar`، `Dashboard`.

### چه چیزی فرق داشت و اصلاح شد

| اختلاف | اصلاح |
|---|---|
| صفحه‌ها ستون باریک وسط بودند (`mx-auto max-w-*`) | ستون تمام‌عرض با گاتر ثابت ۵۶ پیکسل (`px-gutter`) — دقیقاً مثل پروتوتایپ |
| میزکار سربرگ کوتاه و بی‌منو داشت و فوتر نداشت | یک پوسته برای همه: `x-site.header` (۷ مقصد) + `x-site.footer` + ستون کناری ۲۴۰ پیکسلی |
| اندازه‌های تایپوگرافی از Tailwind می‌آمدند، نه از پروتوتایپ | مقیاس معنایی: `text-hero/display/h1/h2/h3/h4/lede/copy/note/stat/metric` |
| صفحه ابزار، نتیجه را کارت آماری معمولی نشان می‌داد | کارت سبز پررنگ با عدد ۶۴ پیکسلی، جعبه «فرمول به‌کاررفته» و بخش «درباره این ابزار» |
| فهرست‌ها کارت پشت کارت بودند | جدول داده با سربرگ، خط ملایم و ردیف پانویس (`x-data-table`) |
| مسیر صفحه دستی در هر قالب نوشته می‌شد | `x-breadcrumb` با آخرین مورد `aria-current` |
| تیتر صفحه بدون بند معرفی و جای دکمه | `x-page-header` با اسلات‌های `lede`، `actions` و `meta` |
| ابزارهای قالب صنعتی با شناسه لاتین نمایش داده می‌شدند | قرارداد `App\Contracts\ToolDirectory` عنوان فارسی را می‌دهد، بدون شکستن قاعده ۱ |

### ایرادهایی که فقط با رندر واقعی پیدا شدند

- `°C` کنار عدد نتیجه وارونه می‌شد؛ عدد و واحد یک بلوک `data-numeric` شدند.
- ستون کناری ۴۰ پیکسل ارتفاع داشت (خود پروتوتایپ هم همین است) — قاعده ۴۴ پیکسلی
  ما بر تطابق پیکسلی مقدم است و به `min-h-touch` تغییر کرد.
- نمودار مقایسه در ستون باریک، ایستگاه پنجم را از دید بیرون می‌انداخت؛ نمودار
  عرض کامل گرفت و کارت‌های آماری زیرش رفتند.
- `select` داخل grid روی موبایل صفحه را ۸۷ پیکسل سرریز می‌کرد (`min-width:auto`).
- **سربرگ مشترک `route('tools.index')` را بدون `Route::has` صدا می‌زد**؛ یعنی
  خاموش‌کردن ماژول ابزارها همه صفحه‌ها را ۵۰۰ می‌کرد. تست
  `tests/Feature/SiteShellTest.php` حالا همین را ثابت نگه می‌دارد.

### شواهد

- `composer run check` سبز: Pint، PHPStan سطح ۶ بدون خطا، ۵۵۱ تست / ۱۸۶۷ ادعا.
- `npm run a11y` روی ۱۴ مسیر (شامل صفحه‌های نیازمند ورود) در سه عرض
  ۳۹۰/۷۶۸/۱۲۸۰: «همه بررسی‌ها قبول».
- استقلال ماژول‌ها دوباره ثابت شد: با `// 'Tools'` تست‌های پروژه‌ها ۲۴/۲۴ و با
  `// 'Projects'` تست‌های ابزارها ۱۳/۱۳ سبز می‌مانند.

### تصمیم‌های آگاهانه که با پروتوتایپ یکی نیستند

1. **ستون کناری فقط مقصدهای واقعی را فهرست می‌کند** (۵ ردیف، نه ۱۴). ردیف مرده
   در ستون کناری کاربر را سردرگم می‌کند. (منوی بالا هم از اصلاح طراحی موبایل به
   بعد فقط بخش‌های ساخته‌شده را نشان می‌دهد؛ «کاریابی» و «مشاوره» با رسیدن
   ماژولشان برمی‌گردند.)
2. **نشان نسخه روی کارت ابزار، «نسخه فرمول» را نشان می‌دهد** (`1.0.0`)، چون
   «نسخه ابزار» مستقل از فرمول در مدل ما وجود ندارد.
3. **متن سلب ادعا از ثابت `Calculation::DISCLAIMER` موتور محاسبات می‌آید**، نه از
   قالب؛ یک منبع، یک متن.
4. **صفحه اصلی فقط قهرمان و نوار اعتمادش هم‌تراز شد**؛ بقیه بلوک‌های آرت‌بورد
   `Main` محتوایی‌اند و کار بخش ۹‌اند.

## بخش ۹ — دانشنامه (تحویل‌شده)

`app/Modules/Encyclopedia` — ششمین ماژول. معیار پذیرش: «محتوای بدون بازبین
منتشر نمی‌شود؛ نسخه چاپی بدون منو و با منابع خروجی می‌دهد.»

### چه ساخته شد

| بخش | چیست |
|---|---|
| شش نوع محتوا | مقاله · راهنما · واژه‌نامه · روش اندازه‌گیری · نمونه موردی · قوانین — هرکدام با دوره بازبینی و کف تعداد منبع خودش |
| `PublishArticle` | تنها دری که وضعیت به «منتشرشده» می‌رسد؛ سه شرط را پیش از نوشتن بررسی می‌کند |
| نشانگر تازگی داده | سه حالت: تازه · نزدیک موعد · نیازمند بازبینی — روی خود صفحه محتوا |
| بلوک ابزار درون متن | شناسه ابزار در بخش، نه تگ در متن؛ با نبودِ ابزار بی‌صدا حذف می‌شود |
| نسخه چاپی | `/encyclopedia/{slug}/print` — بدون منو، بدون دکمه، با منابع نسخه‌دار |
| نمره سلامت محتوا | پنج معیار سنجیدنی، جمع وزن ۱۰۰؛ «کیفیت نوشتار» عمداً در نمره نیست |
| یادآور بازبینی | صفحه Filament «سلامت محتوا»، به ترتیب نمره از کم به زیاد — صف کار، نه گزارش |
| پیوند متقابل | پیوند دستی اول، بعد هم‌نوع‌های منتشرشده |
| `fbh:seed-encyclopedia` | سه محتوای نمونه؛ در production کار نمی‌کند |

### تصمیم‌های کلیدی و دلیلشان

1. **قاعده انتشار در اکشن است، نه در ستون دیتابیس.** پیش‌نویس حق دارد بازبین
   نداشته باشد؛ شرط، وضعیت است نه وجود ردیف. دکمه انتشار پنل مدیریت هم همین
   اکشن را صدا می‌زند — وگرنه قاعده از راه پنل دور زدنی بود.
2. **بخش‌ها ردیف جدا هستند، نه یک ستون HTML.** فهرست «در این مقاله» از خود
   ردیف‌ها ساخته می‌شود و بلوک ابزار یک شناسه است نه یک تگ؛ پس با برداشتن یک
   ابزار، پیوند مرده نمی‌ماند.
3. **موعد بازبینی هنگام انتشار ذخیره می‌شود.** اگر هر بار محاسبه می‌شد، تغییر
   دوره بازبینی در کد، موعد صدها محتوای منتشرشده را بی‌خبر جابه‌جا می‌کرد.
4. **منبع بدون ویرایش و سال، منبع حساب نمی‌شود** — نه در شرط انتشار و نه در
   نمره سلامت. «ISO 9612» ویرایش ۱۹۹۷ و ۲۰۰۹ دو سند متفاوت‌اند.
5. **ارقام سال استناد از خط خود منبع پیروی می‌کنند**: `ISO 9612 (2009)` لاتین،
   «حدود مجاز مواجهه شغلی ایران (۱۴۰۰)» فارسی.
6. **`ToolDirectory` از `titleFor` به `find` تغییر کرد** و حالا `ToolSummary`
   برمی‌گرداند (نام، توضیح، منبع، نسخه). بلوک ابزار درون متن به همه این‌ها نیاز
   داشت و ماژول دانشنامه حق import تعریف ابزار را ندارد.

### شواهد

- `composer run check` سبز: Pint · PHPStan سطح ۶ بدون خطا · **۵۸۲ تست /
  ۱۹۴۴ ادعا** (۳۱ تست تازه).
- `npm run a11y` روی پنج مسیر دانشنامه (فهرست، دو محتوا، نسخه چاپی، فهرست
  فیلترشده) در سه عرض ۳۹۰/۷۶۸/۱۲۸۰: «همه بررسی‌ها قبول».
- رندر واقعی هر سه صفحه با مرورگر بررسی شد و با آرت‌بوردهای `EncyclopediaIndex`،
  `Article` و `ArticlePrint` مقایسه شد.
- استقلال ماژول‌ها: با `// 'Encyclopedia'` هر ۲۹ شکست **فقط** داخل تست‌های خود
  دانشنامه‌اند و هیچ ماژول دیگری نمی‌شکند؛ با `// 'Tools'` تست‌های دانشنامه
  ۳۰/۳۰ سبز می‌مانند (یک تست که موضوعش خود قرارداد ابزارهاست، صریح رد می‌شود).
- تست `ModuleBoundaryTest` روی **همه فایل‌های ماژول** می‌پاید که هیچ کلاسی از
  ماژول دیگری import نشود (قاعده ۱).

### آنچه عمداً ساخته نشد

- **جدول درون متن** — به بخش ۱۰ موکول شد تا با جدول حدود مواجهه بانک مواد
  شیمیایی یک شکل داشته باشد.
- **پیوند خودکار بین‌موضوعی** — فعلاً فقط هم‌نوع. شباهت متنی روی فارسی بدون
  نمایه‌سازی درست، ارجاع بی‌ربط می‌دهد. موتور پیوند داخلی واقعی بخش ۱۷ است.
- **ویرایشگر محتوا در پنل** — محتوا فعلاً با دستور ساخته می‌شود.

### دو تغییر جانبی که لازم شد

- `SeoTest` بدون `RefreshDatabase` اجرا می‌شد و نقشه سایت حالا به جدول محتوا
  نیاز دارد؛ تستی که بدون پایگاه داده اجرا شود، چیزی را می‌سنجد که در واقعیت
  نیست.
- `ModuleSystemTest` از نام «Encyclopedia» به‌عنوان «ماژول غیرفعال» استفاده
  می‌کرد. نام یک ماژول برنامه‌ریزی‌شده در چنین تستی یعنی تست با رسیدن آن بخش
  می‌شکند؛ به نامی عوض شد که هرگز ماژول نمی‌شود.

## اصلاح استایل عقب‌مانده سایت (تحویل‌شده)

گزارش مدیر: «استایل سایت بهم ریخته.» علت پیدا شد، نه حدس زده شد: سایت زنده
CSS‌ای می‌داد که هیچ‌کدام از کلاس‌های تازه بخش هم‌ترازسازی طراحی را نداشت
(`px-gutter`، `text-h1`، `text-display`، `rounded-note`…). قالب‌ها منتقل شده
بودند، CSS‌شان نه — چون تا آن لحظه `public/build` در gitignore بود و هر
استقرار دو قدم داشت (git pull برای کد، آپلود دستی برای دارایی‌ها) و قدم دوم
فراموش شد.

**ریشه بسته شد، نه نشانه:** `public/build` کامیت می‌شود و با همان `git pull`
می‌آید. نگهبانش در CI است — گردش کار `npm run build` می‌زند و اگر نتیجه با
آنچه کامیت شده فرق داشت، شکست می‌دهد. هزینه پذیرفته‌شده: ۱۶۴ کیلوبایت و شش
فایل در diff.

## بخش ۱۰ — بانک مواد شیمیایی (تحویل‌شده)

`app/Modules/Chemicals` — هفتمین ماژول. معیار پذیرش: «حد مواجهه بدون منبع
منتشر نمی‌شود؛ ورود CSV قبل از اجرا گزارش تغییرات می‌دهد.»

### چه ساخته شد

| بخش | چیست |
|---|---|
| `Domain\CasNumber` | رقم کنترلی شماره CAS را بررسی می‌کند؛ روی هشت شماره واقعی پروتوتایپ تست شده |
| `PublishSubstance` | تنها دری که وضعیت به «منتشرشده» می‌رسد؛ سه شرط را پیش از نوشتن بررسی می‌کند |
| `LimitAuthority` / `LimitType` | چهار مرجع و چهار نوع حد — IDLH با نشان صریح «غیرقابل مقایسه با اندازه‌گیری معمول» |
| `CsvImporter::plan()` | فقط می‌خواند؛ گزارش تازه/به‌روزرسانی (با فیلد پیش/پس)/بدون‌تغییر/نامعتبر |
| `ApplyCsvImport` | روی `ImportPlan` تأییدشده می‌نویسد، نه فایل خام؛ ماده تازه همیشه پیش‌نویس می‌ماند |
| `CsvExporter` | خروجی با همان ستون‌های ورودی — رفت‌وبرگشت بدون تغییر ستون تست شده |
| `SubstanceComparer` | جدول مقایسه تا سه ماده — چندمرجعی، نه تک‌مرجعی مثل پروتوتایپ |
| صفحه Filament «ورود CSV مواد» | آپلود، پیش‌نمایش، اجرا، خروجی — با همان جفت پیش‌نمایش/اجرا |

### تصمیم‌های کلیدی و دلیلشان

1. **شماره CAS رقم کنترلی دارد و بررسی می‌شود.** فرمول واقعی رقم کنترلی
   پیاده شد و روی هشت ماده واقعی پروتوتایپ (تولوئن تا منگنز) صحت‌سنجی شد؛
   یک اشتباه تایپی حالا قابل تشخیص است، نه پنهان.
2. **قاعده انتشار در اکشن است، نه در ستون دیتابیس.** همان الگوی بخش ۹:
   پیش‌نویس حق دارد منبع نداشته باشد؛ شرط، وضعیت است نه وجود ردیف.
3. **تشخیص «تازه»/«به‌روزرسانی» در CSV روی شماره CAS است، نه نام.** نام
   فارسی یک ماده در دو فایل می‌تواند جور نباشد؛ CAS یکتا و بدون ابهام است.
4. **`ApplyCsvImport` روی `ImportPlan` تأییدشده می‌نویسد، نه فایل خام** —
   آنچه مدیر در پیش‌نمایش دیده، دقیقاً همان چیزی است که ذخیره می‌شود.
5. **مقایسه‌گر عمداً از پروتوتایپ فاصله گرفت.** پروتوتایپ یک مرجع (ACGIH) را
   نشان می‌داد؛ بانک ما چندمرجعی است، پس هر نوع حدی که دست‌کم یکی از مواد
   دارد یک ردیف می‌گیرد و مقدار از اولین مرجع موجود (اولویت با ایران) می‌آید.
6. **`ToolDirectory` دوباره استفاده شد** (همان قرارداد بخش ۹)، این‌بار برای
   بخش «محاسبه با این ماده» — تبدیل واحد و TWA، عمومی برای همه مواد.

### شواهد

- `composer run check` سبز: Pint · PHPStan سطح ۶ بدون خطا · **۶۱۷ تست /
  ۲۰۰۴ ادعا** (۳۵ تست تازه).
- `npm run a11y` روی سه مسیر در سه عرض ۳۹۰/۷۶۸/۱۲۸۰: «همه بررسی‌ها قبول»
  (دو ایراد واقعی پیدا و رفع شد: هدف لمسی ردیف جدول، سرریز افقی جعبه جست‌وجو
  روی موبایل).
- رندر واقعی هر سه صفحه با مرورگر بررسی و با آرت‌بوردهای `ChemicalIndex`،
  `Chemical` و `ChemCompare` مقایسه شد.
- استقلال ماژول‌ها: با `Chemicals` خاموش، هر ۲۲ شکست فقط داخل تست‌های خود
  این ماژول‌اند؛ با `Tools` خاموش، ۳۵/۳۵ تست بانک مواد سبز می‌ماند.
- یک باگ واقعی پیدا و رفع شد: `HAVING` روی نام مستعار در SQLite کار نمی‌کرد
  (کوئری صف تأیید)؛ با `whereHas` جایگزین شد — تست‌های ماژول Admin همین را
  گرفتند.

### آنچه عمداً ساخته نشد

- **دسته‌بندی موضوعی** (حلال آروماتیک، فلزات سنگین…) — تاکسونومی موضوعی
  هنوز تعریف نشده؛ اختراعش حدس بود، نه تصمیم.
- **صفحه تاریخچه تغییرات** — مدل قرارداد `Revisable` را پیاده می‌کند و
  `RevisionRecorder` هسته آماده است، ولی صفحه نمایش ساخته نشد.
- **بانک داده اولیه واقعی** — تصمیم مدیر: «فعلاً فقط چند ماده نمونه دستی.»

## بخش ۱۱ — هسته مالی (تحویل‌شده)

`app/Modules/Ledger` — هشتمین ماژول. معیار پذیرش: «مجموع هر تراکنش صفر
است؛ اجرای دوباره با همان کلید اثر دوم ندارد.»

### چه ساخته شد

| بخش | چیست |
|---|---|
| `ledger_accounts` / `ledger_transactions` / `ledger_entries` | سه جدول فقط‌افزودنی؛ حساب بعد از ساخته‌شدن هرگز تغییر نمی‌کند |
| `wallets` | تنها جدول تغییرپذیر؛ `cached_balance_toman` فقط از راه `Wallet::applyLedgerEntry()` |
| `Services/LedgerService` (پشت `App\Contracts\LedgerRecorder`) | جمع صفر و Idempotency را در کد سرویس اجرا می‌کند، نه در دیتابیس |
| `App\Support\Ledger\*` | `AccountType`، `EntryDirection` و DTOهای مرز؛ عمداً در Support نه داخل ماژول |
| `LedgerTransactionRecorded` | `AuditableEvent`؛ فقط روی نوشتن تازه منتشر می‌شود، نه بازپخش |
| `Actions/CreditWalletManually` + صفحه Filament «شارژ کیف پول» | تنها راه ورود پول در نسخه یک؛ کاربر با شماره موبایل پیدا می‌شود |
| `fbh:reconcile-wallets` | گزارش/اصلاح مغایرت کش کیف پول با جمع واقعی دفتر کل |

### تصمیم‌های کلیدی و دلیلشان

1. **`AccountType` و `EntryDirection` در `app/Support/Ledger`اند، نه داخل
   ماژول.** هر ماژولی که تراکنش مالی ثبت می‌کند باید بتواند نوع حساب و جهت
   ردیف را مشخص کند؛ اگر این دو Enum داخل فضای‌نام ماژول Ledger می‌ماندند،
   وارد کردنشان از ماژول دیگر قاعده ۱ را می‌شکست.
2. **ناوردای جمع صفر و Idempotency در کد سرویس است، نه Trigger دیتابیس.**
   بررسی پیش از باز کردن تراکنش دیتابیسی انجام می‌شود؛ ضامن نهایی
   Idempotency قید یکتای ستون است، پس حتی در رقابت هم‌زمان دو اثر مالی
   برای یک کلید ثبت نمی‌شود.
3. **نوع حساب ششم `Treasury` اضافه شد** — ADR-0003 پنج نوع را نام برده و
   هیچ‌کدام طرف مقابل شارژ دستی نیست. تصمیم به‌عنوان **DEC-20** ثبت شد و
   منتظر تأیید مدیر است؛ تا آن زمان همین‌طور که هست کار می‌کند.
4. **`Model::preventSilentlyDiscardingAttributes()` در `AppServiceProvider`
   فعال شد** (بیرون از production). بدون آن، مقداردهی گروهی
   `cached_balance_toman` بی‌سروصدا رد می‌شد نه با خطا — کیف پول به رفتار
   بلند تکیه می‌کند تا فراموشی محافظت پنهان نماند.
5. **شارژ دستی کاربر را با شماره موبایل پیدا می‌کند، نه شناسه عددی** —
   مدیر مالی شماره را از تیکت پشتیبانی یا رسید بانکی دارد.
6. **توانایی `admin.wallet.manage` از پیش موجود بود** (بخش ۵، نقش
   `AdminRole::Finance`) و همان‌جا استفاده شد — دوباره اختراع نشد.

### شواهد

- `composer run check` سبز: Pint · PHPStan سطح ۶ بدون خطا · **۶۳۳ تست /
  ۲۰۴۱ ادعا** (۱۶ تست تازه).
- تست مستقیم ناوردای صفر: جمع بستانکار منهای بدهکار هر تراکنش ثبت‌شده در
  کل دیتابیس صفر است (`LedgerServiceTest::test_the_sum_of_every_recorded_transaction_is_zero`).
- تست مستقیم Idempotency: اجرای دوباره یک درخواست با همان کلید نه ردیف
  تازه می‌سازد، نه رویداد Audit دوم می‌فرستد، نه موجودی کیف پول را دوبار
  تغییر می‌دهد.
- صفحه Filament «شارژ کیف پول» با درخواست HTTP واقعی (نه فقط unit) رندر و
  دروازه دسترسی (فقط `Finance`/`Super`) تست شد.
- `ModuleBoundaryTest` این ماژول هم اضافه شد: هیچ فایلی مدل ماژول دیگری را
  import نمی‌کند.

### آنچه عمداً ساخته نشد

- **کمیسیون خودکار، تسویه فروشنده/کارشناس، آداپتور ZarinPal.** این بخش فقط
  زیرساخت دفتر کل را می‌سازد؛ `CommissionService` و `PaymentGateway` بخش
  بعدی‌اند و روی همین دفتر کل بنا می‌شوند.
- **انتقال بین کیف‌پول‌ها، کش‌بک، پرداخت ترکیبی.** ADR-0003 صریحاً این سه را
  از فاز یک بیرون گذاشته.
- **اکشن برگشت/استرداد.** هنوز هیچ عملیات مالی قابل‌برگشتی (مثل پرداخت
  دوره) ساخته نشده؛ منطق برگشت با اولین مصرف‌کننده واقعی می‌آید.
- **رندر و بررسی دسترس‌پذیری با Playwright برای صفحه شارژ کیف پول.** برخلاف
  صفحه‌های عمومی بخش‌های قبل، این یک صفحه یک‌بارمصرف داخل میزکار است و فقط
  با درخواست HTTP واقعی (رندر کامل Blade + Livewire) و آزمون دسترسی/نقش
  تأیید شد، نه مرورگر واقعی — چون هیچ صفحه عمومی یا آرت‌بورد پروتوتایپ برایش
  وجود ندارد که با آن مقایسه شود.

## بخش ۱۲ — تجارت (تحویل‌شده)

`app/Modules/Commerce` — نهمین ماژول. معیار پذیرش: «خرید، بازگشت کامل و
جزئی، کمیسیون و تسویه با هم سازگارند.»

### چه ساخته شد

| بخش | چیست |
|---|---|
| `Product` / `ProductVersion` | چرخه `Draft → InReview → Published/Rejected → Retired`؛ نسخه فقط‌افزودنی با Changelog خودش |
| `Actions/SubmitProductForReview` → `PublishProduct`/`RejectProduct` | صف تأیید یکپارچه (`admin.content.review`)، همان الگوی `ContentHealthPage` دانشنامه |
| `Services/CommissionService` (پشت `App\Contracts\CommissionCalculator`) | مرجع یگانه کمیسیون؛ نرخ تاریخ‌دار از `commission_rates`، Snapshot روی هر ردیف سفارش |
| `App\Contracts\PaymentGateway` + `ZarinPalGateway`/`FakeZarinPalGateway` | تنها کلاسی که ریال می‌بیند؛ کد ۱۰۰/۱۰۱ موفق، مبلغ بازگشتی همیشه با مبلغ سفارش مقایسه می‌شود |
| `Cart` (Session) → `PlaceOrder` → `StartCheckout` → `CompleteOrderPayment` | سفارش پیش از تأیید درگاه هیچ اثر مالی ندارد؛ `idempotency_key` از uuid سفارش |
| `DownloadController` | پیوند امضاشده + بررسی دوباره مالکیت همان لحظه دانلود |
| `Actions/PreviewRefund` / `IssueRefund` | یک منطق مشترک؛ وجه به کیف پول خریدار برمی‌گردد، نه کارت بانکی |
| `Actions/SettleVendor` + `App\Contracts\LedgerBalanceReader` (تازه، در Ledger) | تسویه دستی تا سقف بدهی واقعی؛ خواندن موجودی بدون آستانه خودکار |
| فروشگاه عمومی + پنل فروشنده + سه صفحه Filament (بررسی محصول، بازگشت وجه، تسویه) | پنل فروشنده پیش از میزکار مشترک بخش ۱۵، روی پوسته عمومی |

### تصمیم‌های کلیدی و دلیلشان

1. **`App\Contracts\LedgerBalanceReader` به ماژول Ledger اضافه شد** (نه
   Commerce) — تسویه فروشنده باید بداند چقدر بدهکار است، ولی Commerce حق
   ندارد مدل `LedgerAccount` را import کند. همان مرز `CalculationReader`،
   این‌بار برای خواندن.
2. **طرف بدهکار تراکنش خرید «خزانه» است، نه «حساب واسط درگاه».** ADR-0003
   خودش `gateway_clearing` را فاز آینده می‌داند؛ این تصمیم دامنه DEC-20 را
   از «فقط شارژ دستی» به «هر ورود پول تأییدشده از بیرون سیستم» گسترش داد و
   همان تأیید مدیر را دوباره می‌طلبد.
3. **بازگشت وجه به کیف پول می‌رود، نه به کارت بانکی از راه درگاه** —
   `docs/architecture/admin-panel.md` §۴. در نتیجه خزانه در تراکنش بازگشت
   شرکت نمی‌کند؛ فقط بازتقسیم چیزی است که قبلاً وارد کیف پول پلتفرم شده.
4. **`order_items` مبلغ بازگشتی را کش نمی‌کند** — همیشه از جمع `refunds`
   محاسبه می‌شود. تصمیم آگاهانه برای پرهیز از کش دومی که مثل
   `wallets.cached_balance_toman` نیاز به دستور تطبیق اختصاصی دارد، بدون
   اینکه مسیر پرترافیکی توجیهش کند.
5. **تسویه فروشنده بدون آستانه یا بازه زمانی خودکار** — DEC-17 هنوز باز
   است؛ تا آن تصمیم، مدیر مالی هر زمان و هر مبلغی تا سقف بدهی واقعی را
   دستی تسویه می‌کند (همان الگوی شارژ دستی کیف پول بخش ۱۱).
6. **پنل فروشنده روی پوسته عمومی، نه یک «میزکار» جعلی** — بخش ۱۵ (میزکار
   مشترک با ستون کناری) هنوز ساخته نشده؛ ساختن یک پوسته اختصاصی برای همین
   بخش یعنی دوباره‌کاری وقتی بخش ۱۵ برسد. توانایی‌های `products.manage`،
   `sales.reports`، `settlement.request` از بخش ۵ همان‌طور که بودند
   استفاده شدند.
7. **واترمارک محتوا ساخته نشد.** فایل فروشنده می‌تواند هر قالبی باشد (zip،
   PDF، سند)؛ نشان‌دارکردن قابل‌اعتماد هر قالب یک تصمیم کتابخانه/فرمت جدا
   می‌طلبد که هنوز گرفته نشده — ساختن نسخه‌ای که فقط یک قالب را پوشش
   می‌دهد، ادعای نادرست «واترمارک شد» می‌داد.

### شواهد

- `composer run check` سبز: Pint · PHPStan سطح ۶ بدون خطا · **۶۹۸ تست /
  ۲۱۸۴ ادعا** (۶۴ تست تازه).
- تست کامل جریان خرید با `FakeZarinPalGateway`: پرداخت موفق تراکنش دفتر کل
  متوازن می‌سازد؛ سفارش دو فروشنده دو ردیف بستانکار جدا می‌سازد؛ پرداخت
  لغوشده یا ناموفق هیچ اثر مالی ندارد؛ Callback تکراری دوبار ثبت نمی‌کند.
- تست بازگشت وجه: کامل و دو بازگشت جزئی پیاپی، هر دو با ناوردای صفر و
  به‌روزرسانی درست وضعیت سفارش (`PartiallyRefunded`/`Refunded`)؛ سفارش
  دوفروشنده‌ای تا بازگشت کامل هر دو ردیف در حالت جزئی می‌ماند.
- تست تسویه: کاهش درست بدهی، رد تسویه بیش از بدهی واقعی.
- دانلود امضاشده با درخواست HTTP واقعی تست شد: مهمان و خریدار بدون سفارش
  رد می‌شوند؛ پیوند بی‌امضا رد می‌شود؛ بازگشت کامل وجه دسترسی را باطل
  می‌کند.
- صفحه‌های فروشگاه، پنل فروشنده و هر سه صفحه Filament با درخواست HTTP واقعی
  رندر و دروازه دسترسی (نقش/مالکیت) تست شدند.
- `ModuleBoundaryTest`: هیچ فایلی در Commerce مدل ماژول دیگری را import
  نمی‌کند.

### آنچه عمداً ساخته نشد

- **واترمارک محتوا** — دلیل بالا.
- **درخواست رسمی تسویه از سمت فروشنده** — صفحه فروشنده فقط مانده را نشان
  می‌دهد؛ گردش کار «درخواست» منتظر DEC-17 است.
- **حساب واسط درگاه (`gateway_clearing`)** — فاز آینده طبق خود ADR-0003.
- **کمیسیون/تسویه بازار پروژه (نرخ ۱۰٪ ADR-0003)** — «پروژه» در ADR به
  بازار پروژه (بخش ۲۱، نسخه ۴) اشاره دارد، نه ماژول اندازه‌گیری موجود؛
  هنوز مصرف‌کننده واقعی ندارد.
- **دسته‌بندی موضوعی محصول** — همان وضعیت باز بانک مواد شیمیایی.

## بخش ۱۳ — دوره‌ها (تحویل‌شده)

`app/Modules/Courses` — دهمین ماژول. نقشه راه برای این بخش برخلاف ۱۱، ۱۲
و ۱۴ معیار پذیرش جداگانه ندارد، فقط فهرست موضوعی: «دوره و جلسه و آزمون ·
پیشرفت · محیط یادگیری · دیدگاه · هویت مالی مشترک مدرس و فروشنده». دامنه
این بخش مستقیماً از همان فهرست استخراج شد؛ **این برداشت از دامنه هنوز
تأیید مدیر را نگرفته** — اگر برداشتی غیر از این مدنظر بوده، لازم است
گفته شود.

### چه ساخته شد

| بخش | چیست |
|---|---|
| `Course` / `CourseSession` / `Exam` + سؤال/گزینه | چرخه `Draft → InReview → Published/Rejected → Retired`، دقیقاً الگوی `Product` بخش ۱۲؛ یک دوره حداکثر یک آزمون دارد |
| `Actions/SubmitCourseForReview` → `PublishCourse`/`RejectCourse` | صف تأیید یکپارچه (`admin.content.review`)، همان `Admin/PendingCourses` |
| `Enrollment` (سفارش و ردیف سفارش در یک مدل) | خرید مستقیم بدون سبد — یک ثبت‌نام یعنی یک دوره؛ فیلدهای Snapshot قیمت/کمیسیون |
| `Actions/EnrollInCourse` → `StartCourseCheckout` → `CompleteEnrollmentPayment` | همان `CommissionCalculator` و `PaymentGateway` بخش ۱۲ با `flow='course'`؛ ثبت در دفتر کل با بدهکار خزانه |
| `Services/CourseCompletion` + `Actions/CompleteSession`/`SubmitExamAttempt`/`SubmitCourseReview` | پیشرفت جلسه idempotent، آزمون append-only، تکمیل یک‌طرفه (هرگز پس گرفته نمی‌شود)، دیدگاه فقط بعد از تکمیل |
| کاتالوگ عمومی + پنل مدرس + `CourseReviewPage` (Filament) | پنل مدرس پیش از میزکار مشترک بخش ۱۵، روی پوسته عمومی؛ گزارش فروش مدرس به صفحه تسویه Commerce لینک می‌دهد |

### تصمیم‌های کلیدی و دلیلشان

1. **بدون سبد و بدون مدل `Order` جدا.** یک ثبت‌نام یعنی یک دوره؛ یکی‌کردن
   `Enrollment` با `Order`/`OrderItem` نیازمند نوع محصول چندشکلی بود —
   نامتناسب با دامنه این بخش.
2. **`LedgerAccountRef::vendorPayable($userId)` همان حسابی است که Commerce
   برای فروشنده استفاده می‌کند.** درآمد مدرس و فروشنده یک نفر با هم پول
   می‌شود؛ هیچ صفحه تسویه دومی لازم نبود.
3. **`CommissionCalculator` و `PaymentGateway` از `App\Contracts` وارد
   شدند، پیاده‌سازی از Commerce نه.** `FakePaymentGateway` یک کپی مستقل
   است، نه import — تکرار کوچک ارزان‌تر از شکستن قاعده ۱.
4. **بازگشت وجه دوره ساخته نشد.** خارج از دامنه استخراج‌شده از فهرست
   نقشه راه؛ به فهرست کارهای معوق اضافه شد.
5. **جابه‌جایی جلسه با کشیدن‌وانداختن ساخته نشد.** افزودن ترتیبی ساده
   (`position` صحیح بدون قید یکتایی) برای این بخش کافی بود.
6. **صفحه تسویه اختصاصی مدرس ساخته نشد** — نتیجه مستقیم تصمیم ۲.

### شواهد

- `composer run check` سبز: Pint · PHPStan سطح ۶ بدون خطا · **۷۳۴ تست /
  ۲۲۶۷ ادعا** (۳۶ تست تازه).
- تست کامل خرید دوره با `FakePaymentGateway`: پرداخت موفق تراکنش دفتر کل
  متوازن می‌سازد؛ ثبت‌نام دوگانه رد می‌شود؛ پرداخت لغوشده/ناموفق اثر مالی
  ندارد؛ Callback تکراری دوبار ثبت نمی‌کند.
- تست محیط یادگیری: تکمیل بدون آزمون، تکمیل جلسه idempotent، تکمیل مشروط
  به قبولی آزمون، رد دیدگاه پیش از تکمیل، حداکثر یک دیدگاه، دروازه دسترسی
  روی ثبت‌نام پرداخت‌نشده و غریبه.
- صفحه‌های کاتالوگ، پنل مدرس و صفحه Filament بررسی دوره با درخواست HTTP
  واقعی رندر و دروازه دسترسی (نقش/مالکیت) تست شدند.
- `ModuleBoundaryTest`: هیچ فایلی در Courses مدل ماژول دیگری را import
  نمی‌کند.
- `npm run build` بدون تغییر هش CSS — بدون رانش دارایی.

### آنچه عمداً ساخته نشد

- **بازگشت وجه دوره** — دلیل بالا.
- **جابه‌جایی جلسه با کشیدن‌وانداختن** — دلیل بالا.
- **صفحه تسویه اختصاصی مدرس** — هویت مالی مشترک یعنی صفحه Commerce کافی
  است.

## هم‌ترازسازی دوم با پروتوتایپ (تحویل‌شده)

بازبینی پروتوتایپ («پروتوتایپ کامل فرابهداشت — ۸۲ صفحه») در برابر کد، دو
شکاف واقعی نشان داد. هر دو بسته شد.

### صفحه اصلی واقعی جای صفحه موقت را گرفت

`Main.dc.html` یک صفحه فرود کامل است، ولی مسیر `/` هنوز
`Route::view('/', 'welcome')` بود — سی‌ونه خط با کامنت «صفحه موقت، در بخش ۹
ساخته می‌شود». بخش ۹ تحویل شده بود و این بازنویسی جا مانده بود.

صفحه اصلی حالا از ماژول‌ها ساخته می‌شود، نه از قالب:

- `App\Contracts\HomepageSource` — قرارداد تازه، دقیقاً هم‌الگوی `SitemapSource`.
- `App\Support\Home\HomeSection` و `HomeItem` — فقط رشته، مثل `ToolSummary`.
- `App\Modules\Core\Services\HomePage` — بخش‌های برچسب‌خورده `home.sources` را
  می‌چیند و بخش بدون ردیف را حذف می‌کند.
- پنج ماژول منبع خودشان را ثبت می‌کنند: `ToolHighlights` · `ArticleHighlights` ·
  `SubstanceHighlights` · `ProductHighlights` · `CourseHighlights`.
- مسیر `home` از `routes/web.php` به مسیرهای Core منتقل شد و
  `resources/views/welcome.blade.php` حذف شد.

نتیجه: خاموش‌کردن یک ماژول بخشش را از صفحه اصلی برمی‌دارد و بقیه سر جایشان
می‌مانند (قاعده ۲). سایت تازه‌نصب صفحه اصلی کوتاه می‌بیند، نه قاب خالی.

### مقیاس تایپوگرافی معنایی واقعاً اجرا شد

قالب‌ها ۱۹۳ بار از مقیاس پیش‌فرض Tailwind استفاده می‌کردند (`text-sm` ۱۳۷ بار،
`text-xs` ۷۵ بار) در برابر ۱۱۱ کلاس معنایی — یعنی همان چیزی که `CLAUDE.md`
ممنوعش کرده بود. ریشه‌اش این بود که **مقیاس ناقص بود**: فقط یک پله زیر ۱۵
داشت، درحالی‌که خود پروتوتایپ ۲۲۷ بار از اندازه‌های ۱۱ تا ۱۵ استفاده می‌کند.

دو پله اضافه شد و بعد کل مخزن مهاجرت داده شد:

| پله تازه | اندازه | چرا |
|---|---|---|
| `text-label` | ۱۴ | همان اندازه ردیف فهرست و پیمایش در پروتوتایپ |
| `text-control` | ۱۶ | زیر ۱۶ پیکسل، Safari روی iOS هنگام فوکوس زوم می‌کند |

نگاشت مهاجرت: `text-xs` → `text-note` · `text-sm` → `text-label` ·
`text-base` → `text-control` (کنترل‌ها)، `text-h4` (تیتر)، `text-lede` (بدنه) ·
`text-lg`…`text-5xl` → پله معنایی هم‌اندازه.

بیرون از صفحات Filament، صفر کلاس اندازه پیش‌فرض Tailwind باقی مانده است.

### آنچه عمداً انجام نشد

- **یازده آرت‌بورد مرکز مدیریت** بازنویسی نشد. مرکز مدیریت با Filament ساخته
  می‌شود (ADR-0001) و پوسته خودش را دارد؛ آن آرت‌بوردها مرجع رفتار و چیدمان
  اطلاعات‌اند، نه ظاهر. این در `docs/architecture/design-standards.md` ثبت شد.
  تنها جای مخزن که کلاس رنگ پیش‌فرض Tailwind دارد همین ده فایل Filament است.
- **`Onboarding.dc.html`** (راهنمای اولین ورود) همچنان معوق است — در فاز ۰
  پروتوتایپ است ولی از بخش ۴ به عقب افتاده.
- بقیه شکاف‌های پوشش (۴۷ آرت‌بورد) برنامه‌ریزی‌شده‌اند: بخش‌های ۱۴ تا ۲۱.

### انحراف عمدی از پروتوتایپ

پروتوتایپ Vazirmatn را از Google Fonts می‌گیرد؛ کد آن را خودمیزبان کرده است.
اینجا پروتوتایپ اشتباه است، نه کد: قاعده محصولی می‌گوید هیچ صفحه‌ای داده کاربر
را به سرویس بیرونی نمی‌فرستد — نه فونت، نه آواتار.

## بخش ۱۴ — درآمدزایی (تحویل‌شده)

`app/Modules/Monetization` — یازدهمین ماژول. معیار پذیرش: «خاموش‌کردن اشتراک
بدون تغییر کد هیچ صفحه‌ای، رفتار کل سایت را عوض می‌کند.» مدیر گفت «پیشنهاد
خودتو انجام بده»؛ پیشنهادها در `طرح-بخش-۱۴-درآمدزایی.md` (فایل پروژه) بود.

### چه ساخته شد

| بخش | چیست |
|---|---|
| `App\Contracts\EntitlementGate` + `app/Support/Entitlement` | لایه واحد دسترسی؛ پیش‌فرض همیشه‌مجاز `OpenGate` در `AppServiceProvider` — ADR-0006 |
| `EntitlementResolver` + `QuotaCounter`های برچسب‌دار | سقف رایگان از شمارنده ماژول صاحب داده (`SavedCalculationQuota`، `ProjectQuota`) |
| `Plan` / `Subscription` / `SubscriptionPeriod` + خرید با زرین‌پال | ثبت دفتر کل خزانه ← درآمد پلتفرم، idempotent؛ لغو تا پایان دوره |
| `TeamSeat` | فقط اعطا/لغو مدیر از صفحه «اشتراک‌ها» |
| `/pro` و `/pro/upgrade` | صفحه پلن‌ها و گذرگاه تبدیل با دلیل توقف |
| `revenue_streams` + `ToggleRevenueStream` + `ShutdownPreview` | کلید یازده جریان، چهار عدد پیش از خاموشی، دفتر رویداد برای هر تغییر |
| `SubscriberDiscount` | ۱۰٪ تخفیف مشترک روی فایل، از کمیسیون پلتفرم |

### تصمیم‌های کلیدی و دلیلشان

1. **DEC-13:** ۵ محاسبه ذخیره‌شده و ۱ پروژه برای کاربر رایگان.
2. **DEC-14:** ۲۹۰٬۰۰۰ ماهانه و ۲٬۹۰۰٬۰۰۰ سالانه (دو ماه رایگان)؛ از پنل قابل تغییر.
3. **سیاست پیش‌فرض خاموشی «ادامه تا پایان دوره».** قطع فوری با مشترک فعال در اکشن رد می‌شود.
4. **دسترسی از `ends_at` می‌آید، نه `status`.** cron خراب دسترسی اضافه نمی‌دهد.
5. **تخفیف از سهم پلتفرم، نه فروشنده.**
6. **`StreamRegistry` بدون جدول هم کار می‌کند** (`Schema::hasTable`)؛ فاصله
   `git pull` تا `migrate` در استقرار سایت را ۵۰۰ نمی‌کند.
7. **زمان‌بندی `fbh:expire-subscriptions` در provider ماژول است** نه
   `routes/console.php`، تا حذف ماژول دستور بی‌صاحب جا نگذارد.

### شواهد

- `composer run check` سبز: Pint · PHPStan سطح ۶ بدون خطا · **۷۷۳ تست /
  ۲۴۲۳ ادعا** (۳۸ تست تازه در ماژول، به‌علاوه تغییر تست‌های Refund و RoundComparison).
- `AcceptanceCriterionTest`: خاموش‌کردن کلید سقف‌ها را در ابزار و پروژه برمی‌دارد،
  `/pro` به خانه می‌رود و ردیف منو حذف می‌شود؛ مسیر `OpenGate` همه را مجاز می‌کند.
- `npm run build` بدون تغییر هش CSS.

### آنچه عمداً ساخته نشد

- خرید صندلی توسط خود کاربر (نسخه ۲)، بسته آزمون (فقط کلید، نسخه ۱٫۱).
- اجرای خودکار بازگشت وجه نسبت‌به‌مدت هنگام خاموشی — عددش فقط نمایش داده می‌شود.
- تمدید خودکار — درگاه فعلی پرداخت تکرارشونده ندارد.
- بازگشت وجه اشتراک.

### استقرار

`git pull origin main` و `bash scripts/deploy.sh` (migrate را خودش اجرا می‌کند).
پنج جدول تازه و یک ستون `order_items.discount_toman`. cron `schedule:run` از
قبل روی هاست هست.

## بخش ۱۵ — میزکار و صفحات عمومی (تحویل‌شده)

`app/Modules/Workspace` — دوازدهمین ماژول. طرح در `طرح-بخش-۱۵-میزکار.md` (فایل
پروژه)؛ مدیر «تایید» داد. کدهای تصمیم در طرح ۱۵ تا ۱۹ بودند ولی آن شماره‌ها
در `decisions-pending.md` از قبل معنای دیگری داشتند، پس DEC-21 تا DEC-25 شدند.

### چه ساخته شد

| بخش | چیست |
|---|---|
| `/workspace` | کارت‌های برچسب‌خورده از پنج ماژول، بر اساس نمای جاری (شخصی یا هر پروفایل فعال) — DEC-21 |
| `/workspace/notifications` + زنگ سربرگ | اعلان درون‌سایتی از هر `UserNotifiableEvent` (هشت رویداد در پنج ماژول) — DEC-22 |
| `/workspace/wallet` | موجودی و صورت‌حساب فقط‌خواندنی از `WalletStatementReader` (Ledger) |
| `/status` + صفحه پنل | شش سرویس، نوار ۴۵ روزه، اشتراک رفع اختلال — DEC-23 |
| `/legal/*` + صفحه پنل | چهار سند، نسخه فقط‌افزودنی، پذیرش دوباره برای نسخه اساسی — DEC-24 |
| `/search` + فرم ۴۰۴ | پنج منبع (دانشنامه، مواد، ابزار، دوره، فروشگاه)، CAS دقیق مستقیم — DEC-25 |
| پوسته | زنگ و دکمه میزکار در سربرگ، ردیف‌های تازه ستون کناری، پیوند حقوقی و وضعیت در فوتر؛ پنل فروشنده و مدرس به پوسته میزکار منتقل شدند |

### تصمیم‌های کلیدی و دلیلشان

1. **برچسب‌ها ثابت روی قراردادند** (`WorkspaceWidgetSource::TAG`، `SearchSource::TAG`)
   نه روی provider میزکار، تا حذف این ماژول ثبت‌کننده‌ها را نشکند.
2. **متن اعلان ذخیره می‌شود**، نه از نو ساخته؛ پس از حذف ماژول فرستنده هم خواناست.
   مقصد نام مسیر است و با `Route::has` محافظت می‌شود.
3. **جست‌وجو با گونه‌های پرسش**، نه ستون یکسان‌شده: هر واژه در هر دو شکل ی/ک
   پرسیده می‌شود؛ هیچ ماژولی مهاجرت تازه لازم نداشت.
4. **ورود یعنی پذیرش نسخه‌های جاری** — تیک صفحه ورود از قبل همین را می‌گفت؛
   `AcceptLegalOnSignIn` ثبتش می‌کند. پس فقط کاربری که حین نشست با نسخه اساسی
   تازه روبه‌رو شود، صفحه پذیرش را می‌بیند.
5. **میان‌افزار حقوقی فقط روی مسیرهای `auth`** و هرگز در «مشاهده به‌عنوان کاربر».
6. **سربرگ موبایل:** زیر ۶۴۰ پیکسل آیکون جست‌وجو و کلید حالت تاریک پنهان
   می‌شوند تا زنگ و «میزکار» سرریز افقی نسازند.

### شواهد

- `composer run check` سبز: Pint · PHPStan سطح ۶ بدون خطا · **۸۳۴ تست / ۲۶۰۹ ادعا**
  (۵۷ تست تازه در ماژول، به‌علاوه `ModuleBoundaryTest`).
- `npm run a11y` روی ۱۱ صفحه (واردشده، سه عرض): همه قبول.
- `npm run build` — CSS تازه در `public/build`.

### آنچه عمداً ساخته نشد

- راهنمای اولین ورود، علاقه‌مندی‌ها، شارژ آنلاین کیف پول، اعلان پیامکی/ایمیلی.
- پایش خودکار سرویس‌ها (پس از تصمیم Cron هاست).

### استقرار

`git pull origin main` و `bash scripts/deploy.sh`. شش جدول تازه. پس از
استقرار، مدیر ارشد باید از «اسناد حقوقی» نسخه ۱ قوانین و حریم خصوصی را منتشر
کند؛ تا آن موقع صفحات حقوقی خالی‌اند و کسی متوقف نمی‌شود؛ پس از آن، کاربرانی که در نشستند یک‌بار صفحه پذیرش را می‌بینند.

## بخش ۱۶ — گزارش‌ساز (ساخته‌شده)

`app/Modules/Reports` — سیزدهمین ماژول. طرح در `طرح-بخش-۱۶-گزارش-ساز.md` (فایل
پروژه)؛ مدیر «تأیید» داد. تصمیم‌ها DEC-26 تا DEC-29.

### چه ساخته شد

| بخش | چیست |
|---|---|
| `/workspace/reports/*` | گزارش‌ساز چهارمرحله‌ای: منبع · مشخصات · یافته‌ها · بازبینی و صدور؛ پیش‌نویس در هر مرحله ذخیره می‌شود |
| `app/Contracts/ReportSource` | منبع گزارش را ماژول صاحب داده ثبت می‌کند: Projects (پروژه با تجهیزات) و Tools (چند محاسبه ذخیره‌شده) |
| صدور | Snapshot منجمد، شناسه `FBH-XXXX-XXXX`، PDF یک‌باره با mPDF و هش SHA-256 — DEC-26، DEC-27 |
| `/verify` | صفحه عمومی تأیید اصالت با محدودیت نرخ؛ فقط وضعیت و فراداده و هش — DEC-28 |
| اصلاح و ابطال | اصلاح = نسخه تازه که قبلی را «جایگزین‌شده» می‌کند؛ ابطال به دست مالک یا مدیر محتوا |
| پنل و میزکار | صفحه Filament «گزارش‌های صادرشده» (`admin.reports.manage`)، کارت میزکار، ردیف ستون کناری، اعلان صدور |

### تصمیم‌های کلیدی و دلیلشان

1. **پیش‌نمایش و صدور یک قالب و یک شیء (`ReportDocument`) دارند**؛ آنچه کاربر
   در پیش‌نمایش دید دقیقاً همان است که صادر می‌شود.
2. **هشدار کالیبراسیون جلوی صدور را نمی‌گیرد، تأیید می‌خواهد** — همان قاعده
   بخش ۸ — و تأیید روی خود PDF چاپ می‌شود.
3. **پیش‌نویس و پیش‌نمایش برای کاربر رایگان آزاد است، صدور Pro** (DEC-29)؛
   گزارش صادرشده با پایان اشتراک از دسترس نمی‌رود.
4. **صفحه تأیید محتوا و نام کارفرما را نشان نمی‌دهد** (DEC-28)؛ گیرنده فایل را
   دارد و فقط هش لازم دارد.
5. **`direction: rtl` صریح در CSS برگه**؛ بدون آن mPDF تاریخی را که با رقم شروع
   می‌شود وارونه می‌چید. با رندر واقعی PDF دیده و اصلاح شد.
6. **`App\Modules\Reports\Tests` در `autoload-dev`** تا تست‌ها یک trait داده نمونه
   مشترک داشته باشند (پوشه `tests` با حرف کوچک از نگاشت `App\` پیدا نمی‌شود).

### شواهد

- `composer run check` سبز: Pint · PHPStan سطح ۶ بدون خطا · **۸۷۲ تست**
  (۳۸ تست تازه در Reports و دو منبع Projects/Tools، به‌علاوه `ModuleBoundaryTest`).
- PDF صادرشده و پیش‌نمایش رندر و بصری بازبینی شد: چسبیدن حروف، جهت، QR، شماره
  صفحه فارسی، نشان «پیش‌نمایش».
- `npm run build` — CSS تازه در `public/build`.

### آنچه عمداً ساخته نشد

نمودار مقایسه در PDF (جدول همان داده را دارد) · لوگوی سازمان · قالب سفارشی و
مشترک تیمی · تک‌فروشی گزارش · امضای دیجیتال · ارسال ایمیلی.

### استقرار

`git pull origin main` و `bash scripts/deploy.sh`. یک جدول تازه (`reports`) و
وابستگی تازه `mpdf/mpdf` (از `composer.lock`؛ افزونه‌های `gd` و `mbstring` لازم دارد که هر دو در فهرست `docs/host-quick-check.md` هستند). پوشه‌های `storage/app/private/reports`
و `storage/framework/cache/mpdf` خودکار ساخته می‌شوند و باید قابل نوشتن باشند.

## بخش ۱۷-الف — سئو و پیوند داخلی (تحویل‌شده)

طرح در `طرح-بخش-۱۷-سئو-و-تست-پذیرش.md` (فایل پروژه)؛ مدیر «تأیید» داد. بخش
۱۷ دو تحویل دارد و این اولی است. تصمیم‌ها DEC-30 تا DEC-36 (۳۳ تا ۳۶ مال ۱۷-ب).

### چه ساخته شد

| بخش | چیست |
|---|---|
| Schema همه صفحات عمومی | صفحه اصلی `Organization`+`WebSite` با جست‌وجو · مقاله `Article` · واژه‌نامه `DefinedTerm` · ماده `ChemicalSubstance` · ابزار `WebApplication` · دوره `Course` با قیمت · محصول `Product` · صفحه حقوقی `WebPage` · مسیر راهنما روی همه صفحه‌های جزئیات · Canonical روی همه فهرست‌ها |
| `SeoMeta` و `Schema` | از `Modules/Core/Seo` به `app/Support/Seo` رفتند تا Courses و Commerce و Workspace بی‌import کردن Core از آن‌ها استفاده کنند |
| نقشه سایت بخش‌بندی‌شده | `/sitemap.xml` فهرست فایل‌هاست؛ `pages` · `encyclopedia` · `chemicals` · `tools` · `courses` · `shop`. ابزار، دوره، محصول و صفحات حقوقی تازه آمدند. بخش بزرگ خودکار چند فایل می‌شود |
| `Modules/Linking` | موتور پیوند داخلی: تطبیق فارسی با مرز کلمه، ذخیره پیوند هنگام بازسازی، «مقاله‌هایی که به این اشاره دارند» زیر ماده و ابزار، «در این مطلب» کنار مقاله، صفحه مدیر با یتیم‌ها و مسدودسازی |
| `tests/Feature/PublicSeoTest.php` | از نقشه سایت هر نشانی را باز می‌کند: ۲۰۰، بدون noindex، Canonical خودش، یک H1، توضیح، JSON-LD معتبر و بدون ادعای اعتبار یا امتیاز |

### تصمیم‌های کلیدی و دلیلشان

1. **پیوند هنگام بازسازی ساخته و ذخیره می‌شود** (DEC-31)؛ تطبیق همه عبارت‌های
   سایت روی هر بازدید پرهزینه است و پیوند ذخیره‌شده گزارش‌پذیر است.
   `scripts/deploy.sh` پس از کش بازسازی را اجرا می‌کند.
2. **پیوند خودکار تک‌به‌تک تأیید نمی‌شود** (DEC-32)؛ فقط تطبیق دقیق، و مدیر
   مسدود می‌کند. هر مسدودسازی بازسازی را همان لحظه اجرا می‌کند.
3. **مترادف ماده مقصد پیوند نیست** و عبارتی که به دو مقصد بخورد کنار می‌رود؛
   پیوند غلط از نبودنش بدتر است.
4. **نیم‌فاصله مرز کلمه است ولی بین کلمه‌های یک عبارت با فاصله برابر است**؛
   «صوت» در «صوت‌سنج» پیوند نمی‌خورد، «ماسک نیم صورت» «ماسک نیم‌صورت» را پیدا
   می‌کند. پسوند جدانوشته و «ی» اضافه پس از ا و و جزو کلمه است.
5. **`JSON_HEX_TAG` در JSON-LD**: عنوان دوره و محصول را فروشنده می‌نویسد و بدون
   آن «&lt;/script&gt;» در عنوان از تگ بیرون می‌زد. تست دارد.
6. **`changefreq` و `priority` از نقشه سایت حذف شدند**؛ گوگل نمی‌خواندشان.

### شواهد

- `composer run check` سبز: Pint · PHPStan سطح ۶ بدون خطا · **۸۹۹ تست** (۲۷ تست تازه:
  ۶ نگهبان سئوی کل سایت، ۴ نقشه سایت، ۱۱ تطبیق عبارت، ۸ پیوند از بازسازی تا صفحه و
  پنل، ۱ مرز ماژول؛ منهای ۳ تست قدیمی changefreq/priority).
- `npm run a11y` روی مقاله پیوندخورده، ماده، ابزار و صفحه اصلی در سه عرض قبول.
  اولین اجرا پیوند درون متن را «هدف لمسی کوچک» گرفت؛ `py-2.5` روی پیوند درون‌خطی
  ناحیه لمس را ۴۴ پیکسل کرد بی‌آنکه ارتفاع خط عوض شود.
- صفحه مقاله، ماده و ابزار با پیوند واقعی رندر و بصری بازبینی شد
  (`/mnt/project-files/section-17/`).
- طرح گفته بود پیوند «پرش به محتوا» در پوسته نیست؛ اشتباه بود — در
  `layouts/base` از قبل هست.

### استقرار

`git pull origin main` و `bash scripts/deploy.sh`. سه جدول تازه (`link_targets`،
`internal_links`، `link_blocks`)؛ deploy.sh پیوندها را خودش بازسازی می‌کند.

## بخش ۱۷-ب — تست پذیرش نسخه ۱ (ساخته‌شده)

گزارش کامل با شواهد: `docs/acceptance/v1/README.md`؛ بخش دستی (پرداخت واقعی،
گوشی واقعی، Lighthouse، Rich Results): `docs/acceptance/v1/manual-checklist.md`؛
چک‌لیست خروج از staging در `docs/deployment.md`.

| محور | نگهبان خودکار |
|---|---|
| مالی | `tests/Feature/Acceptance/FinancialScenarioTest` — سناریوی سرتاسری با ناورداهای دفتر کل پس از هر گام، ریال فقط در آداپتور، هیچ عملیات مالی در حالت مشاهده |
| RTL | `RtlStaticTest` (کلاس فیزیکی در Blade) · عدد و واحد بیرون از `data-numeric` در `a11y-check.mjs` |
| موبایل، کیبورد، دسترس‌پذیری | `scripts/a11y-check.mjs`: سه عرض، axe-core محلی، پیمایش Tab (پیوند پرش اول، حلقه فوکوس، تله فوکوس). **در CI** روی سرور با محتوای نمونه، عمومی و میزکار |
| سرعت | `PerformanceBudgetTest`: CSS ≤ ۶۰ و JS ≤ ۵۰ کیلوبایت، ≤ ۲۵ پرس‌وجو در صفحه عمومی، `preventLazyLoading` روی همه نشانی‌های نقشه سایت |
| سئو | `PublicSeoTest`: عنوان ≤ ۶۰ و توضیح ≤ ۱۶۰؛ `PublicPathShadowTest`: هیچ مسیری هم‌نام `public/` نیست |

### ایرادهایی که پذیرش پیدا کرد

1. **خرید در حالت «مشاهده به‌عنوان کاربر» باز بود.** `guardAgainstFinancialAction`
   هیچ‌جا صدا زده نمی‌شد. حالا قرارداد `App\Contracts\FinancialGuard` (پیش‌فرض
   `AlwaysAllowed`، ماژول Admin آن را با `Impersonation` بازنویسی می‌کند)، میان‌افزار
   `financial` روی سه مسیر پرداخت و لغو تمدید، و خود `StartCheckout`،
   `StartCourseCheckout` و `StartSubscriptionCheckout`. خطا `FinancialActionBlocked`
   از نوع ۴۰۳ است.
2. **N+1 در فهرست مواد**؛ `SubstanceFinder::search` حالا `limits` و `facts` را از
   پیش بار می‌کند.
3. `text-left` در صفحه چاپ ابزار → `text-end`.
4. عنوانی که با « — فرابهداشت» از ۶۰ نویسه می‌گذشت، حالا بی‌نام سایت چاپ می‌شود.

### تصمیم‌ها و دلیلشان

- **کف طول عنوان و توضیح در CI نیست**، فقط سقف. توضیح کوتاه کار محتواست و در
  گزارش پذیرش فهرست شده؛ شکستن CI برایش هر PR بی‌ربطی را قرمز می‌کرد.
- **N+1 با `Model::preventLazyLoading` گرفته می‌شود، نه فقط با شمارش پرس‌وجو**:
  با داده نمونه کم، سقف ۲۵ هیچ‌وقت نمی‌شکند؛ بارگذاری تنبل در حلقه با دو ردیف هم
  خطا می‌دهد. برای همین نمونه‌ها دوتایی‌اند (`tests/Feature/SeedsPublicSite`).
- **بررسی مرورگری در همان کار CI**، نه کار جدا: وابستگی‌ها و دارایی ساخته‌شده
  دوباره نصب نمی‌شوند.
- ورودی تاریخ چهار Tab می‌خورد و فوکوس دکمه تقویمش در سایه مرورگر است؛ پیمایش
  کیبورد این را تله یا «بدون حلقه» نمی‌شمارد.

### شواهد

`composer run check` سبز: ۹۱۱ تست. `npm run a11y` روی ۱۴ صفحه عمومی و ۱۰ صفحه
پس از ورود (میزکار و Pro) در سه عرض بدون ایراد. بیشترین پرس‌وجو ۹، CSS ۴۳ کیلوبایت، JS ۱٫۲ کیلوبایت.

## مرحله بعد

**خروج از staging** — تصمیم جدای مدیر (DEC-36) پس از
`docs/acceptance/v1/manual-checklist.md`. Cron هر دقیقه فعال است (HOST-05 بسته شد،
۲۰۲۶-۰۹-۲۵). هنوز از مدیر لازم است: سقف‌های LVE از Resource Usage (HOST-06).

دو مورد معوق قبلی (سرویس آپلود مدیا، راهنمای اولین ورود)، دو مورد بانک مواد
(دسته‌بندی موضوعی، صفحه تاریخچه تغییرات)، دو مورد تجارت (واترمارک محتوا،
درخواست رسمی تسویه) و دو مورد دوره (بازگشت وجه دوره، جابه‌جایی جلسه) همچنان
معوق‌اند. صندوق اعلان در بخش ۱۵ ساخته شد.

DEC-20 با دامنه گسترش‌یافته (خزانه، هم برای شارژ دستی هم برای پرداخت
تأییدشده درگاه) هنوز باز است.

باز مانده و بی‌ربط به تحویل این بخش:

- پرسش‌های میزبانی در `docs/decisions-pending.md` (HOST-01/02/04/05/06/07 و نسخه
  MySQL/MariaDB) — پیش از بخش ۱۷ لازم است.
- Alpine.js در ADR-0001 انتخاب شده ولی هنوز نصب نیست.
- سرویس آپلود مدیا: جدول و مدل `media_items` آماده است، آپلودکننده نه.

## تصمیم‌های کلیدی و دلیلشان

1. **قاعده انتشار در اکشن است، نه در ستون دیتابیس.** پیش‌نویس حق دارد بازبین
   نداشته باشد؛ شرط، وضعیت است نه وجود ردیف. دکمه انتشار پنل مدیریت هم همین
   اکشن را صدا می‌زند — وگرنه قاعده از راه پنل دور زدنی بود.
2. **بخش‌ها ردیف جدا هستند، نه یک ستون HTML.** فهرست «در این مقاله» از خود
   ردیف‌ها ساخته می‌شود و بلوک ابزار یک شناسه است نه یک تگ؛ پس با برداشتن یک
   ابزار، پیوند مرده نمی‌ماند.
3. **موعد بازبینی هنگام انتشار ذخیره می‌شود.** اگر هر بار محاسبه می‌شد، تغییر
   دوره بازبینی در کد، موعد صدها محتوای منتشرشده را بی‌خبر جابه‌جا می‌کرد.
4. **منبع بدون ویرایش و سال، منبع حساب نمی‌شود** — نه در شرط انتشار و نه در
   نمره سلامت. «ISO 9612» ویرایش ۱۹۹۷ و ۲۰۰۹ دو سند متفاوت‌اند.
5. **ارقام سال استناد از خط خود منبع پیروی می‌کنند**: `ISO 9612 (2009)` لاتین،
   «حدود مجاز مواجهه شغلی ایران (۱۴۰۰)» فارسی.
6. **`ToolDirectory` از `titleFor` به `find` تغییر کرد** و حالا `ToolSummary`
   برمی‌گرداند (نام، توضیح، منبع، نسخه). بلوک ابزار درون متن به همه این‌ها نیاز
   داشت و ماژول دانشنامه حق import تعریف ابزار را ندارد.

### شواهد

- `composer run check` سبز: Pint · PHPStan سطح ۶ بدون خطا · **۵۸۲ تست /
  ۱۹۴۴ ادعا** (۳۱ تست تازه).
- `npm run a11y` روی پنج مسیر دانشنامه (فهرست، دو محتوا، نسخه چاپی، فهرست
  فیلترشده) در سه عرض ۳۹۰/۷۶۸/۱۲۸۰: «همه بررسی‌ها قبول».
- رندر واقعی هر سه صفحه با مرورگر بررسی شد و با آرت‌بوردهای `EncyclopediaIndex`،
  `Article` و `ArticlePrint` مقایسه شد.
- استقلال ماژول‌ها: با `// 'Encyclopedia'` هر ۲۹ شکست **فقط** داخل تست‌های خود
  دانشنامه‌اند و هیچ ماژول دیگری نمی‌شکند؛ با `// 'Tools'` تست‌های دانشنامه
  ۳۰/۳۰ سبز می‌مانند (یک تست که موضوعش خود قرارداد ابزارهاست، صریح رد می‌شود).
- تست `ModuleBoundaryTest` روی **همه فایل‌های ماژول** می‌پاید که هیچ کلاسی از
  ماژول دیگری import نشود (قاعده ۱).

### آنچه عمداً ساخته نشد

- **جدول درون متن** — به بخش ۱۰ موکول شد تا با جدول حدود مواجهه بانک مواد
  شیمیایی یک شکل داشته باشد.
- **پیوند خودکار بین‌موضوعی** — فعلاً فقط هم‌نوع. شباهت متنی روی فارسی بدون
  نمایه‌سازی درست، ارجاع بی‌ربط می‌دهد. موتور پیوند داخلی واقعی بخش ۱۷ است.
- **ویرایشگر محتوا در پنل** — محتوا فعلاً با دستور ساخته می‌شود.

### دو تغییر جانبی که لازم شد

- `SeoTest` بدون `RefreshDatabase` اجرا می‌شد و نقشه سایت حالا به جدول محتوا
  نیاز دارد؛ تستی که بدون پایگاه داده اجرا شود، چیزی را می‌سنجد که در واقعیت
  نیست.
- `ModuleSystemTest` از نام «Encyclopedia» به‌عنوان «ماژول غیرفعال» استفاده
  می‌کرد. نام یک ماژول برنامه‌ریزی‌شده در چنین تستی یعنی تست با رسیدن آن بخش
  می‌شکند؛ به نامی عوض شد که هرگز ماژول نمی‌شود.

## تصمیم‌های کلیدی و دلیلشان

| تصمیم | دلیل |
|---|---|
| Laravel به‌جای Next.js | سازگاری بومی با cPanel؛ نبود PostgreSQL، محدودیت اینود و RAM مرحله Build |
| Blade برای صفحات عمومی به‌جای SPA | سئو و سرعت روی هاست اشتراکی؛ بدون هزینه Build |
| Filament برای پنل مدیر | جایگزین واقعی «مدیریت بدون کدنویسی» وردپرس |
| دفتر کل دوطرفه به‌جای ستون موجودی | سازگاری بازگشت وجه، کمیسیون و امانت‌داری وجه پروژه |
| واحد پایه پول **تومان** با ستون BIGINT و پسوند `_toman` | تصمیم مدیر؛ ریال فقط داخل آداپتور درگاه تا ابهام هرگز وارد دامنه نشود |
| موتور محاسبات به‌صورت پکیج مستقل | قابلیت بازتولید گزارش و تست طلایی مستقل از UI |
| افزودن اشتراک Pro | مدل درآمدی اولیه فقط تک‌فروشی بود و درآمد تکرارشونده نداشت |
| نرخ کمیسیون با تاریخ اثر | جلوگیری از خراب‌شدن صحت گزارش‌های مالی گذشته |
| بانک مواد شیمیایی به‌صورت داده، نه کد | تکمیل تدریجی محتوا بدون هیچ تغییری در برنامه |
| لایه واحد دسترسی (Entitlement) | خاموش‌کردن هر جریان درآمدی بدون بازنویسی کد صفحات |
| حالت تاریک فقط برای ابزار و میزکار | نیاز میدانی شیفت شب؛ صفحات سئویی روشن می‌مانند |
| بررسی خودکار دسترس‌پذیری در CI | بررسی چشمی در ۷۴ صفحه دوام نمی‌آورد |
| قاعده انتشار محتوا در اکشن، نه در ستون دیتابیس | پیش‌نویس حق دارد بازبین نداشته باشد؛ شرط، وضعیت است نه وجود ردیف |
| بخش‌های محتوا به‌صورت ردیف جدا، نه یک ستون HTML | فهرست مقاله و بلوک ابزار بدون تجزیه HTML ساخته می‌شوند و پیوند مرده نمی‌ماند |
| موعد بازبینی هنگام انتشار ذخیره می‌شود | تغییر دوره در کد نباید موعد محتوای منتشرشده را بی‌خبر جابه‌جا کند |
| شماره CAS با رقم کنترلی‌اش بررسی می‌شود | اشتباه تایپی در ورود CSV باید قابل تشخیص باشد، نه پنهان تا روز استناد |
| ورود CSV گزارش تغییرات می‌سازد و روی همان گزارش می‌نویسد | آنچه مدیر پیش‌نمایش دیده باید دقیقاً همان چیزی باشد که ذخیره می‌شود |
| مقایسه‌گر مواد چندمرجعی است | تک‌مرجعی‌کردن جدول، داده واقعی بانک چندمرجعی را پنهان می‌کند |
| دارایی‌های ساخته‌شده (`public/build`) در گیت کامیت می‌شوند | هاست اشتراکی npm ندارد؛ استقرار دوقدمی یعنی قدم دوم فراموش می‌شود |
| `AccountType` و `EntryDirection` در `app/Support/Ledger`اند، نه داخل ماژول | هر ماژولی که تراکنش مالی ثبت می‌کند باید این دو را بدون شکستن قاعده ۱ مشخص کند |
| ناوردای جمع صفر و Idempotency در کد سرویس، نه Trigger دیتابیس | قابل تست، قابل خطای صریح (`InvalidArgumentException`)، مستقل از نوع دیتابیس (MySQL/SQLite) |
| `Model::preventSilentlyDiscardingAttributes()` بیرون از production فعال شد | بدون آن، نوشتن ستون خارج از `$fillable` بی‌سروصدا رد می‌شد؛ کیف پول به رفتار بلند تکیه دارد |
| کمیسیون یک سرویس واحد است، پشت `App\Contracts\CommissionCalculator` | فروشگاه و دوره‌ها (بخش ۱۳) باید یک نرخ ببینند؛ محاسبه پراکنده یعنی روزی دو نرخ متفاوت |
| طرف بدهکار خرید «خزانه» است، نه «حساب واسط درگاه» | ADR-0003 خودش gateway_clearing را فاز آینده می‌داند؛ دامنه DEC-20 گسترش یافت، نه نادیده گرفته شد |
| بازگشت وجه به کیف پول می‌رود، نه به کارت بانکی | ساده‌تر از بازگشت درگاه، و مطابق تصمیم مستند admin-panel.md §۴ |
| مبلغ بازگشتی order_items کش نمی‌شود | جمع refunds همیشه محاسبه‌پذیر است؛ کش دوم بدون دستور تطبیق اختصاصی توجیه ندارد |
| پنل فروشنده پیش از میزکار مشترک روی پوسته عمومی است | بخش ۱۵ هنوز نساخته شده؛ پوسته اختصاصی موقت یعنی دوباره‌کاری |
| واترمارک محتوا ساخته نشد | فرمت فایل فروشنده متغیر است؛ پوشش‌دادن فقط یک فرمت ادعای نادرست می‌داد |
| ثبت‌نام دوره (`Enrollment`) هم سفارش است هم ردیف سفارش، بدون سبد | یک ثبت‌نام یعنی یک دوره؛ نوع محصول چندشکلی برای یکی‌کردن با `Order` نامتناسب دامنه بود |
| درآمد مدرس و فروشنده یک `vendorPayable` مشترک است | هویت مالی مشترک؛ صفحه تسویه Commerce بدون تغییر برای مدرس هم کار می‌کند |
| صفحه اصلی از قرارداد `HomepageSource` ساخته شد، نه پرس‌وجوی مستقیم | صفحه اصلی به پنج ماژول نگاه می‌کند؛ import مستقیم یعنی شکستن قاعده ۱ در پربازدیدترین صفحه سایت |
| دو پله `text-label` و `text-control` به مقیاس اضافه شد | مقیاس ناقص بود و قالب‌ها ناچار به `text-sm` پناه می‌بردند؛ ممنوع‌کردن بدون جایگزین، قاعده را کاغذی می‌کند |
| فیلدها ۱۶ پیکسل ماندند، نه ۱۵ پروتوتایپ | زیر ۱۶ پیکسل Safari روی iOS هنگام فوکوس زوم می‌کند — همان قاعده‌ای که هدف لمسی را بر تطابق پیکسلی مقدم می‌داند |
| صفحات Filament از توکن‌ها پیروی نمی‌کنند | داخل پوسته Filament رندر می‌شوند؛ هماهنگی واقعی یعنی تم Filament، نه بازنویسی صفحات |
| بازگشت وجه دوره ساخته نشد | خارج از دامنه استخراج‌شده از فهرست نقشه راه بخش ۱۳؛ به فهرست معوق افزوده شد |
| `EntitlementGate` همیشه بسته است، با پیش‌فرض `OpenGate` | برداشتن ماژول درآمدزایی خودکار سایت را بی‌محدودیت می‌کند؛ پرسنده هرگز null چک نمی‌کند |
| دسترسی اشتراک از `ends_at`، نه ستون وضعیت | لغو تا پایان دوره معتبر می‌ماند و cron خراب دسترسی اضافه نمی‌دهد |
| تخفیف مشترک از کمیسیون پلتفرم کم می‌شود | فروشنده هزینه تصمیم درآمدی پلتفرم را نمی‌دهد |
| مهارت‌های طراحی Impeccable و Redesign فقط به‌صورت Markdown در `.claude/skills` آمدند، بدون اسکریپت و هوک | لانچر Impeccable باینری دانلود می‌کند و بعد از هر ویرایش اجرا می‌شود؛ کد بررسی‌نشده در نشست‌ها اجرا نمی‌شود. قواعد `CLAUDE.md` بر آن‌ها مقدم است |

## تصمیم‌های باز — نیازمند پاسخ مدیر

مرجع کامل: `docs/decisions-pending.md`
