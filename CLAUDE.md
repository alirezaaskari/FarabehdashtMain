# راهنمای کار روی مخزن فرابهداشت

این فایل برای هر کسی است که روی این کد کار می‌کند — انسان یا دستیار.

## پشته

Laravel 13 · PHP 8.3 · MySQL/MariaDB · Filament 5 (از بخش ۵) · Blade + Tailwind (از بخش ۲)
· Pest 5 · PHPStan سطح ۶ (Larastan) · Pint

## ساختار

```
app/Modules/<Name>/   ماژول‌ها، هرکدام با ساختار یکسان و README خودش
app/Support/          ابزارهای مشترک بدون منطق تجاری
app/Contracts/        رابط‌های بین‌ماژولی
packages/calc-engine/ موتور محاسبات، پکیج مستقل بدون Laravel (بخش ۶)
config/modules.php    فهرست ماژول‌های فعال
docs/                 ADRها، معماری و نقشه راه
```

## هفت قاعده غیرقابل نقض

۱. **هیچ ماژولی Model ماژول دیگر را import نمی‌کند.** فقط Interface در `app/Contracts` یا Event.
۲. **هر ماژول یک ServiceProvider دارد.** حذف ماژول = حذف پوشه + یک خط از `config/modules.php`.
۳. **هر ماژول README دارد** و بدون خواندن کد بقیه قابل فهم است.
۴. **منطق تجاری در Controller و Blade نوشته نمی‌شود** — فقط Action و Service.
۵. **هر وضعیت یک Enum است**، نه رشته جادویی.
۶. **پول همیشه عدد صحیح تومان** با پسوند `_toman` در نام ستون و از مسیر `Money`.
۷. **هر تغییر داده حساس یک Event منتشر می‌کند** تا Audit Log مستقل بماند.

## قواعد محصولی که هرگز نقض نمی‌شوند

- هیچ خروجی ابزاری ادعای تشخیص پزشکی، تأیید ایمنی قطعی یا انطباق قانونی قطعی ندارد.
- گواهی و نشان داخلی، مدرک رسمی یا گواهی OSHA/NIOSH معرفی نمی‌شود.
- کارجو هرگز برای دیدن آگهی یا ارسال درخواست پول نمی‌دهد.
- اطلاعات تماس کارجو بدون اجازه صریح او نمایش داده نمی‌شود؛ بانک رزومه Opt-in است.
- محتوای فروشنده، مدرس، مشاور و کارفرما پیش از انتشار نیازمند تأیید مدیر است.
- موجودی کیف پول بدون تراکنش ثبت‌شده تغییر نمی‌کند و فقط مدیر آن را شارژ می‌کند.
- ریال فقط داخل آداپتور درگاه پرداخت دیده می‌شود؛ بقیه سیستم تومان است.

## قواعد لایه طراحی

- هیچ رنگ هگزی در قالب‌ها نوشته نمی‌شود؛ فقط کلاس معنایی (`bg-surface`، `text-muted`، `bg-primary`).
  کلاس‌های رنگی پیش‌فرض Tailwind هم ممنوع‌اند.
- خصوصیت منطقی به‌جای فیزیکی: `ps-*` · `pe-*` · `ms-*` · `me-*` · `start-*` · `end-*`.
- عدد و واحد علمی `data-numeric` می‌گیرند تا در متن راست‌چین وارونه نشوند: مقدار
  اندازه‌گیری، شماره موبایل، کد و شناسه لاتین و چپ‌به‌راست می‌مانند.
- عدد داخل جمله فارسی با دستور `@fa($value)` چاپ می‌شود تا ارقام فارسی باشد
  («۲ دقیقه»). دو قاعده بالا هرگز با هم قاطی نمی‌شوند.
- حالت تاریک فقط میزکار و ابزارها؛ صفحات عمومی با `theme="light"` ثابت می‌مانند.
- هر کامپوننت باید شش حالت داشته باشد: Loading · Empty · Error · Success · Disabled · Permission.
- هدف لمسی هیچ‌گاه کمتر از ۴۴ پیکسل نیست (۴۸ در فرم، ۵۴ در ابزار میدانی).

## دستورها

```bash
composer run lint       # بررسی قالب کد
composer run lint:fix   # اصلاح خودکار قالب
composer run analyse    # تحلیل ایستا
composer run test       # تست‌ها
composer run check      # هر سه با هم — همان چیزی که CI اجرا می‌کند

php artisan fbh:make-module <Name>   # ساخت اسکلت ماژول تازه

npm run build           # ساخت دارایی‌ها
npm run a11y            # بررسی خودکار دسترس‌پذیری
```

## استاندارد کد

`declare(strict_types=1)` در همه فایل‌ها · کلاس‌ها به‌صورت پیش‌فرض `final` ·
تزریق وابستگی از سازنده، نه Facade در منطق تجاری · نام‌ها به انگلیسی، متن کاربر به فارسی ·
هر ماژول تست خودش را در `app/Modules/<Name>/tests` دارد.

## پیش از هر Pull Request

`composer run check` باید سبز باشد.
